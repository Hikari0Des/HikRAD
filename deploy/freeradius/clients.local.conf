# Empty by default — $INCLUDEd from clients.conf. Copy
# clients.local.conf.example here (or edit in place) to add a one-off NAS
# client, e.g. for TEST_NAS_IP. This file is real config, always parsed;
# an empty file (just comments) is a no-op.
