{
  "name": "@hikrad/frontend",
  "private": true,
  "version": "0.1.0",
  "//": [
    "npm workspace root (Phase 1, Agent 5 / F — contract C9).",
    "'panel' entry added per Agent 4's merge note 1 (frontend/panel/package.json).",
    "CI (\".github/workflows/ci.yml\") runs from this directory:",
    "  npm ci && npm run lint --workspaces --if-present && npm run build --workspaces --if-present && npm run i18n:check"
  ],
  "workspaces": ["shared", "panel", "portal"],
  "scripts": {
    "i18n:check": "node shared/scripts/i18n-check.mjs",
    "lint": "npm run lint --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
