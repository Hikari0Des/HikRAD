// Logical-properties rule shared with @hikrad/shared (contract C8 / NFR-6.2).
// Adopted 2026-07-18 (v2 phase 12, C9) — the panel was previously the one
// app not enforcing this; portal and @hikrad/shared always did.
import shared from '../shared/stylelint.config.mjs'

export default shared
