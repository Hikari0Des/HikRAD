# Known issues & fixed-bug log

> Purpose (owner request 2026-07-16, item 11): every bug found in HikRAD — fixed or still open — gets a row here with its root cause, so future work (human or AI) never has to guess at symptoms that were already diagnosed. **Append-only discipline: add rows, update Status, never delete.** v2 phases are required to maintain this file (docs/v2/phases/00-v2-execution-plan.md rule 3).

Format: newest first inside each section. Cite the fixing commit once pushed; deeper war stories live in the phase gate results linked below.

## Open / by-design limitations

| Found | Area | Issue | Root cause | Status / workaround |
|---|---|---|---|---|
| 2026-07-18 | Panel / forms (`frontend/panel/src/components/form/Field.tsx`) | Across the panel's ~40 `Field`-wrapped form screens, the visible label text is not programmatically associated with its input — `getByLabelText`/screen-reader "what field is this" both fail — because `Field` renders `<label htmlFor={htmlFor}>` as a **sibling** of its `children`, but almost every call site never passes `htmlFor` (or a matching `id` on the control). Found writing `SubscriberFormModal.test.tsx` (v2 phase 12): `screen.getByLabelText(en.subscriber.password)` threw "Found a label... however no form control was found associated to that label." — not a fluke, `Field`'s own source has always worked this way. **Pre-existing since Phase 1** (`Field`'s `htmlFor` prop has always been optional, and it was already optional/unused at most call sites before v2-12's control-set rewrite — this phase changed `Field`'s internals from Tailwind-on-native to nothing at all in this regard, it inherited the gap unchanged) — not introduced by the Radix rebuild. | `Field` was written as a wrapper that renders a label and, separately, whatever `children` the caller passes — it relies on the caller opting in to `htmlFor`/`id` for the association, and most callers never did, since a sighted developer testing by eye can't see the missing link (the label visually sits right above the field either way). | **Partially fixed**: `SubscriberFormModal.tsx` (14 `Field`/control pairs) and `frontend/portal/src/pages/SettingsPage.tsx` (5 pairs) gained explicit `id`/`htmlFor` in this phase, since both were already being touched. **Not fixed repo-wide** — roughly 35 other `Field` call sites across the panel (NasWizardModal, ManagersPage, every reports/billing filter bar, etc.) still lack the association; out of scope for a frontend-*styling* phase to sweep every screen's markup. Worth a dedicated small pass: grep `<Field label=` for call sites missing `htmlFor`, add matching `id`s — mechanical, low-risk, no visual change. |
| 2026-07-18 | Settings / branding (`internal/portalapi/branding.go`, `internal/radius/hotspot.go`, `internal/billing/receipt.go`) (**FIXED — v2 phase 11**) | The public `/api/v1/branding` endpoint, the MikroTik Hotspot captive-portal page, and printed receipts had **never actually shown a configured ISP identity** on any install — all three silently fell back to the hardcoded "HikRAD"/"HikRAD Wi-Fi" default regardless of what Settings > Branding had saved. Found while researching v2 phase 11 (instance branding) kickoff, reading every existing branding consumer before freezing the phase's contracts. | Three independent bugs, all present since these features were first built: (1) `internal/portalapi/branding.go`'s `brandingHandler` and `internal/radius/hotspot.go`'s `loadBranding` both called `platform.Get[brandingSettings](ctx, settings, "branding")` — a single settings key `"branding"` that has **never existed**, since `platform.Settings` (`backend/internal/platform/settings.go`) stores one row per fully-qualified key (`branding.name`, `branding.logo_url`, `branding.primary_color`, `branding.secondary_color` — what `setupapi/settings_api.go`'s `branding` group and the admin UI, `BrandingSettings.tsx`, actually read/write). `GetRaw` on a nonexistent key returns `ErrSettingNotFound`, both callers swallowed the error and fell through to their hardcoded default struct. (2) Even fixing the key granularity, both readers' struct field names (`color_primary`, `logo_data_uri`) didn't match the real stored field names (`primary_color`, `logo_url`) — a second, independent mismatch. (3) `internal/billing/receipt.go`'s `branding()` read `billing.receipt_branding` expecting a JSON **object** (`{name, address, phone}`), but the only UI that ever wrote that key, `BillingSettings.tsx`, sends a plain **boolean** (a "show branding on receipt" toggle) — `json.Unmarshal` failed, the error was silently swallowed exactly like (1), and `renderReceipt` fell to its own hardcoded `"HikRAD"` literal. Nothing in the test suite exercised any of the three end-to-end (each had only handler-level tests against an unset/default settings state), so none of this ever surfaced. | **Fixed**: `internal/platform.LoadIdentity` (new, `internal/platform/branding.go`) is the single corrected reader every consumer now calls — reads the four `branding.*` keys independently, never a nonexistent combined key. The Hotspot builder inlines the logo as a base64 `data:` URI read straight off disk (`platform.ReadLogoBytes`), never a fetchable URL (NFR-7). `billing.receipt_branding` is now correctly read as the boolean it always was (`showReceiptBranding`); the dead `Address`/`Phone` fields (never wired to any UI) were retired. Logo storage moved off the settings JSONB row onto local disk (`platform.StoreLogo`/`DeleteLogo`, `HIKRAD_BRANDING_DIR`, default `data/branding`), validated server-side for the first time (1 MiB ceiling, magic-byte content sniffing, 2048px dimension cap, SVG `<script>` rejection) via `POST`/`DELETE /api/v1/settings/branding/logo`. See `docs/v2/phases/phase-v2-11-instance-branding/gate-result.md` (GREEN 39/40) for the regression tests proving each of the three original symptoms is gone. |
| 2026-07-18 | API / `internal/httpapi` (`router.go`, `enforceJSON`) (**FIXED — v2 phase 11**) | The global `enforceJSON` middleware — applied to **every** mutating route in the API — rejected **any** `multipart/form-data` request with `415 unsupported_media_type`, unconditionally. Found while wiring `POST /api/v1/settings/branding/logo` (v2 phase 11, FR-91), whose contract requires multipart, and getting a `415` through the real router even though the handler itself was correct. | `enforceJSON` (`internal/httpapi/router.go`) checked `strings.HasPrefix(ct, "application/json")` only — a `multipart/form-data; boundary=...` Content-Type never matches, so the check fails and the request is rejected before any handler runs. This was not new to this phase: `internal/billing/ticket_attachments.go`'s payment-proof upload (v2-2, FR-78.2) and `internal/portalapi`'s `submitTicketHandler` both accept multipart today and have since v2-2 shipped — but **every existing test exercising them calls the Go function directly** (`billing.SubmitTicket`, per `internal/billing/tickets_gate_test.go`'s own comment: "straight through billing.SubmitTicket... the same exported seam portalapi..."), bypassing the HTTP router and this middleware entirely. Nothing in the existing test suite had ever sent a real multipart request through the live server before this phase's own tests did — so a subscriber attempting to actually attach a transfer-proof screenshot via the real API would have always gotten a `415`, silently, since the feature shipped. | **Fixed**: `enforceJSON` now accepts a `Content-Type` prefix of `multipart/form-data` alongside `application/json`. Verified no regression: `TestContentTypeEnforced` and the full `internal/httpapi` suite stay green (`text/plain` and other non-JSON, non-multipart bodies are still rejected). See `docs/v2/phases/phase-v2-11-instance-branding/gate-result.md` for the fix commit and verification detail. **Worth a follow-up**: confirm whether `internal/portalapi`'s payment-ticket-attachment upload has ever actually worked in the field, or whether this bug means it hasn't — not investigated further here, out of this phase's scope, but flagged since it's a real customer-facing flow (FR-78.2), not just this phase's own new endpoint. |
| 2026-07-18 | Tests (reports) | `TestSiteMarginNeverBlendsGlobal` (`internal/reports/margin_gate_test.go`) fails with `insert nas: ERROR: duplicate key value violates unique constraint "nas_ip_key"` when run as part of the full `internal/reports` DB-gated suite (or the full repo suite) — passes when run alone as the only test in the process, immediately after a fresh database. Found while gating v2-10 (customizable dashboards), which touches neither `internal/reports` nor NAS creation. | The test inserts a NAS row with a **hardcoded literal IP** rather than a `uniq()`-randomized one (the convention every other DB-gated test in this codebase follows specifically to survive sharing a database with sibling tests — see the `TestRevenueReportReconcilesWithPayments` row below for the same species of bug, different root cause: a wall-clock window there, a literal IP here). Some other test in the same package/run plants a NAS row at that literal IP first; this test's own insert then collides. | Open, test-only, not a v2-10 regression (confirmed: reproduces identically against a freshly created database, as part of a full-suite run, on the pre-v2-10 commit too — isolating this one test on its own against a fresh database, with no sibling test running first, passes every time). Workaround: run `internal/reports` DB-gated tests in isolation if this one specifically needs to pass. Proper fix = randomize the NAS IP the same way every other test in this suite already does; not attempted here since it's pre-existing and outside v2-10's scope. |
| 2026-07-18 | Tooling / `scripts/gate-v2-phase-*.sh` `check_go_test` | Every prior v2 gate script's `check_go_test` helper (`gate-v2-phase-1.sh` through `gate-v2-phase-9.sh`) greps `go test -v`'s output for the bare substring `PASS` with no `SKIP` — but `go test -run <pattern>` prints exactly that (`PASS`, exit 0, no `SKIP` anywhere) when the pattern matches **zero tests**, not only when a named test genuinely ran and passed. Found while writing `gate-v2-phase-10.sh` fresh (its DB-gated legs reference test function names — `TestDashboardWidgetsPermissionGating` etc. — that don't exist yet, by design, since the phase brief was frozen before any feature code): a first dry run of the new script reported 8 fake `[PASS]`es for tests that were never written, confirmed by manually reproducing `go test -run TestThisDoesNotExistAtAll12345 -v` → `PASS` / `ok ... [no tests to run]`. | `case "$out" in *SKIP*) ... ; *PASS*) pass ;; esac` treats the bare summary line, not a per-test result line, as proof of execution — `go test` always prints a trailing `PASS`/`FAIL` summary even when the `-run` filter matched nothing. | **Fixed in `gate-v2-phase-10.sh` only**: `check_go_test` now requires a literal `--- PASS: <name>` line (and treats `--- FAIL:`/`--- SKIP:` lines as failing, same intent as before). **Not retroactively fixed in `gate-v2-phase-1.sh` through `gate-v2-phase-9.sh`** — out of scope for this phase; those scripts' *already-published* gate-result PASSes were independently spot-checked at the time (real `--- PASS:` lines were observed manually during each phase's own verification), so no prior gate result is believed to be a false positive from this specific flaw, but a **future re-run** of any of them (e.g. after a test gets renamed and the script's `-run` pattern goes stale) could silently go green without anyone noticing. Worth applying the same one-line fix to the older scripts next time one of them is touched. |
| 2026-07-18 | Ops / `install.sh` | A non-root `install.sh --no-start` run (e.g. `HIKRAD_SKIP_OS_CHECK=1` for local/CI testing, bypassing the root-privilege gate that normally blocks this) **silently aborts** partway through step 3d, before the CLI wrapper, cron entry, or — as of this phase — the `hikrad-updaterd` systemd unit ever get installed. Found while empirically testing this phase's `install.sh` changes in a real (non-root) WSL2 Ubuntu shell, then confirmed against a pristine `git archive` checkout of the commit before this phase — pre-existing, not a regression from this phase's edits. | `chown 10002:10002 "$HIKRAD_ROOT/data/acct-spill"` (and the sibling `chown 10001:10001 .../clients-generated.conf` right after it) requires `CAP_CHOWN` (root) to reassign ownership to an arbitrary uid; run unguarded under the script's `set -euo pipefail`, so a non-root invocation exits there with no error message beyond the bare `chown: ... Operation not permitted` line — the script simply stops, `--no-start` notwithstanding. | **Open, not fixed in this phase** (out of scope for FR-86–88; this is v1.1-era code, `set -e` interacting with a real root requirement). Whether CI's `scripts` job (`.github/workflows/ci.yml`, `install.sh idempotency`, which also sets `HIKRAD_SKIP_OS_CHECK=1`) actually reaches/passes this step depends on whether `ubuntu-latest`'s default runner context can chown to an arbitrary uid — not verified here. This phase's own new steps (hikrad-updaterd binary + systemd unit, step 4a/4b) are all individually guarded against a non-root failure (matching this file's own established pattern), but sit **after** step 3d in file order, so they were never actually reached during this non-root reproduction — verified separately with `sudo` instead (see the gate result). Worth a real fix (guard the two `chown` calls the same way every other privilege-sensitive step in this file already is) next time someone is in this area. |
| 2026-07-18 | Tests (portalapi) | `TestMockGatewayLifecycleReplayAndDisabled` and `TestPortalCardPaymentSubmitAndQueue` (`internal/portalapi/db_test.go`) fail with `404 resource not found` — reproduces on every run against a fresh DB. Found incidentally running the full backend regression suite while verifying v2-7 (one-click updater) introduced no collateral damage; `internal/portalapi` is untouched by v2-7, and by this discovery's own diagnosis the two tests exercise routes that no longer exist for an unrelated, older reason. | v2-2 (manual payment providers) **retired the entire Phase-4 e-wallet gateway surface** — `PaymentGateway`, the mock adapter, `payment_intents`/`gateway_configs`, and migrated `card_payments` into `payment_tickets` (migrations 0586/0587) — "deleted, not quarantined," per its own gate result. These two test files were never updated or removed when that happened, so they still call the old mock-gateway and card-payment routes, which correctly 404 (the routes are genuinely gone) rather than the tests failing for a reason related to what they're actually testing. | Open — pre-existing since v2-2 shipped (2026-07-17), not a v2-7 regression (v2-7 touches none of `internal/portalapi`, `internal/billing`'s gateway/card-payment code, or the routes involved). Fix is straightforward whenever someone is next in `internal/portalapi`: delete or rewrite these two tests against the current `payment_tickets` surface (likely a direct FR-79/FR-78 analog already covered elsewhere in the suite — worth checking for duplicate coverage before just deleting). |
| 2026-07-18 | Ops / `hikrad-updaterd` (**FIXED, caught before ship**) | Drafted the updater socket's directory as `0770` root-owned in the phase brief (v2 phase 7, FR-86) — would have made `hikrad-api`'s container `connect()` fail with a plain permission error on every real install, since the daemon runs as root (systemd, no `User=`) and the container connects as its own unrelated non-root uid; no relationship between the two, so `770` grants the container nothing. Caught during implementation, before any commit shipped it, by recognizing it as the exact same class of bug already on this page for the pre-existing `radius-control` bind mount (freeradius uid vs. hikrad-api uid). | The original C1 text reasoned "only two parties ever need to reach this socket... token auth is the real boundary" and concluded no `chmod 777` was needed — true for the *security* boundary, but it skipped the *OS-level* one: token auth happens only after `connect()` already succeeds, and `connect()` itself is gated by ordinary filesystem permissions the two uids don't share. | Fixed before the wiring commit landed: directory `0777` (`scripts/install.sh`), socket file `0777` (`backend/cmd/hikrad-updaterd/main.go`, chmod'd right after `net.Listen`) — same reasoning `radius-control` already documents: no secret lives in an ephemeral IPC path, the token is what actually authenticates each request. Phase brief C1 amended in place to match. |
| 2026-07-18 | Ops / `hikrad update` (**FIXED**) | `hikrad update`'s image rollback (on a failed `compose up` or a failed post-update health wait) was **silently a no-op**: the previous image ids were snapshotted, but re-tagging them back never actually restored what `docker compose up` would look for. Found while wiring v2 phase 5's bundle-mode update path — bundle mode's `compose.yml` pins a real, meaningful version tag per release (`ghcr.io/hikrad/hikrad-api:vX.Y.Z`), which made the pre-existing gap concretely observable rather than a latent correctness issue. | `cmd_update`'s per-service loop computed `ref="$(compose config --images | grep "^${svc}")"` — intended to capture each service's actual resolved image reference — but `docker compose config --images` prints bare image names with no service-name prefix, so the `grep "^${svc}"` filter never matched anything and `ref` was always empty. The loop never used `ref` anyway: it unconditionally wrote a **hardcoded, unrelated** `"hikrad-$svc:latest=$id"` line instead. `rollback_update_images` then dutifully re-tagged the old image id onto that fabricated name, which was never the name `docker compose up` actually resolves the service to in either build mode. | Fixed (`scripts/hikrad`, `cmd_update`): the snapshot now reads each image's own current `RepoTags[0]` via `docker inspect` — the literal tag `docker compose up` will look for, correct in both source mode's `build:`-synthesized name and bundle mode's pinned registry tag. Also added a one-deep `release_dir.rollback` backup/restore for bundle-mode updates specifically, since a bundle's `compose.yml` itself changes per version — image-id rollback alone cannot rescue a failed bundle update without the compose file reverting too. |
| 2026-07-18 | Tests (reports) | `TestRevenueReportReconcilesWithPayments` fails intermittently — `totals[IQD]=X, want Y` — only when the full `internal/billing` + `internal/reports` DB-gated suites are run back-to-back against the shared `hikrad_test` database in one invocation; passes 100% of the time run alone or as the only reports test. Found running the v2-2 gate's full-regression leg. | The test windows its reconciliation query to `[now-1min, now+1min]` and treats that as "only this test's own data" — but the shared, never-reset test database means any OTHER test's payment rows created in the previous ~60 seconds (e.g. `internal/billing`'s own renewal tests, which create real `payments` rows with `at=now()`) also fall inside that window and get summed by both the direct query and the report endpoint. When billing's payment-creating tests finish less than a minute before this one starts, its `directTotal` and the report's total should still agree (both query the same window) — the actual discrepancy suggests a second package's tests were still landing rows between the two queries, not a real reconciliation bug in the report itself. Not the same root cause as the `internal/accounting` chaos-suite row below, but the same species: a shared, never-reset test database plus a test that implicitly assumes it owns "right now." | Open, test-only, not a v2-2 regression (confirmed: the same test passes in isolation before and after v2-2's changes, and v2-2 touches neither `payments`, `revenue_daily`, nor the reports package). Workaround: run `internal/reports`'s DB-gated suite on its own, or accept a brief pause between packages. Proper fix = scope the reconciliation query to rows this test itself created (e.g. tag with a per-test marker) rather than a wall-clock window shared with every other DB-gated test in the run; not attempted here since it's pre-existing and outside this phase's scope. |
| 2026-07-17 | monitorsvc / dashboard (**FIXED**) | The panel dashboard's "revenue today" tile has always silently shown 0, on every install, since it was built. Found while making `revenue_daily` currency-aware (v2 phase 4) and re-reading `revenueToday`'s fallback-column-probing loop. | The query's `WHERE` clause filtered on a column named `day`; `revenue_daily` (migration 0201) has always named that column `date`. Every candidate in the amount-column probing loop shares the same broken `WHERE`, so all five attempts failed identically with `column "day" does not exist` (42703) — caught by the same `isUndefinedTable` fallback that's supposed to handle a genuinely-missing *amount* column, so the bug never surfaced as an error, only as a tile that was always exactly 0. | Fixed: `WHERE date = ...` (`backend/internal/monitorsvc/dashboard_api.go`). Also scoped to `currency = 'IQD'` as part of the same edit (see the next row) since the view is now grouped by currency too. |
| 2026-07-17 | monitorsvc / alerts (**FIXED**) | The `agent_balance_low` alert rule has likely never fired on any install. Found while renaming `manager_balances.balance_iqd` (v2 phase 4). | `agentBalanceLow`'s query was `SELECT manager_id::text, name, balance_iqd FROM manager_balances` — with no `JOIN`, even though `manager_balances` has never had a `name` column (that's on `managers`). The function's own error handling silently no-ops on ANY query error ("degrades cleanly while D's schema lands," per its doc comment), so the column-not-found error was swallowed exactly like an intentional missing-view case, and nothing ever logged or alerted that the rule itself was broken. | Fixed: joins to `managers` for `name`, and scoped to `currency = 'IQD'` (v2 phase 4, FR-70.2 — balances are now per-currency; a blended-currency low-balance alert would be meaningless). Non-IQD low-balance alerting is a future enhancement, not attempted here. |
| 2026-07-17 | Tests (monitorsvc) | `TestSubscriberEvents_RenewedDeliversWhatsAppReceipt` fails with "timed out waiting for the WhatsApp fake to receive the receipt request" — reproduces consistently, both inside a full `-p 1` run and re-run alone (3/3), against a fresh throwaway DB/Redis pair. Found incidentally while gating v2 phase 3 (this package is untouched by that phase; `internal/radius` itself was fully green in the same run); reconfirmed during v2 phase 4's full-suite pass. | **Not fully diagnosed — flagged rather than guessed.** Plausible mechanism, unconfirmed: the test publishes to Redis pub/sub (`billing.renewed`) after a fixed `time.Sleep(150 * time.Millisecond)` meant to let `subscriberEvents.run`'s goroutine finish subscribing first; pub/sub (unlike a stream) delivers only to already-subscribed listeners, so a slow subscribe on a loaded machine would silently drop the publish with no error on either side — which matches the symptom exactly. Not verified by adding instrumentation, so treat as a hypothesis. **Confirmed pre-existing and unrelated to the v2 phase 4 currency rename**: reproduced identically (3/3) against the commit immediately before v2 phase 4's `renewedEvent.AmountIQD`→`Amount`+`Currency` rename, by temporarily checking out that commit's `subscriber_events.go`/`subscriber_events_test.go` into the same working tree and re-running — same timeout, same message. | Open — belongs with whoever next touches `internal/monitorsvc` or the WhatsApp/push notification path. If the hypothesis is right, the fix is waiting for a real subscribe-confirmation signal instead of a fixed sleep. |
| 2026-07-17 | Migrations / process (**FIXED before release**) | The documented migration numbering — "v1.x maintenance continues at 0412+, v2 uses 0500–0589" — **stopped working the moment v2 phase 1 shipped**: a new maintenance migration numbered 0413 would be silently skipped on every updated install. Caught during this session's verify step, before anything was deployed; the symptom would have been a missing `managers.disabled` column breaking every login after `hikrad update`, with migrations reporting "already up to date". | golang-migrate (`platform.Migrate`, contract C9) is **strictly linear**: `m.Up()` applies only versions **greater than** the database's current version, which on the pilot is already **0504**. Fresh installs would get the 04xx file (they replay the whole sequence); updated installs never would — the worst kind of skew, because CI and dev machines (fresh DBs) stay green. The two-range scheme was designed when v1.x fixes always landed *before* the next phase's numbers; sequential v2 execution broke that assumption invisibly. | The new migration ships as **0505** (v2 phase 1's unused tail). **Rule from now on (docs updated): migration numbers are one linear sequence — every new migration, maintenance or phase, takes the next free number above the repo's current maximum.** Per-phase ranges in the v2 plan remain as budgets/guidance, but a range the sequence has already passed is dead. |
| 2026-07-17 | Panel / dashboard (**FIXED**) | Clicking a NAS device on the dashboard landed on a page titled by its raw id/uuid instead of its name (owner report; pulled forward from v3.4 same day). | `NasStatusPage` (the dashboard card's `/nas/:id/status` target) rendered `title={id}` straight from the route params — the page never fetched the NAS it was showing. `DeviceStatusPage` had the identical bug for monitored devices. | Fixed: both pages resolve the entity (NAS via `getNas`; device from `listDevices`, which has no single-getter) and title with its name + IP subtitle. `StatusView` gained a `queryKey` prop so the title settling from ''→name doesn't refetch probe history. |
| 2026-07-17 | Panel+portal / layout (**FIXED (audit) — v2 phase 12**) | Legacy (OS-chrome) scrollbar appears when the page can't fit the viewport; general responsiveness rough edges on smaller screens (owner report). | Not diagnosed per-screen at the time — symptom class is fixed-width/overflowing containers scrolling the body instead of wrapping or scrolling internally. No repo-wide responsive audit was ever an FR before v2-12 (FR-95) committed one. | **Audited in v2-12**: every `<table>`/`<pre>`/`min-w-[...]` container in both apps (17 files) was already correctly wrapped in `overflow-x-auto`/`overflow-auto` by the time this phase ran — the underlying overflow bug this row describes appears to have been incidentally fixed by ordinary engineering discipline across the phases since 2026-07-17, not by a single targeted fix. A regression-locking `layoutSmoke.test.tsx` (both apps) now scans source for the same invariant and fails CI if a future screen adds a wide element without the wrapper — verified to actually catch a regression (a deliberately-reverted wrapper was confirmed to fail the test before being restored). The modernization's other half (FR-94: dropdowns/fields/ticks, zero native chrome) also shipped this phase. **Not verified**: a real-browser/device visual matrix at 360/768/1280 × light/dark × LTR/RTL — this environment has no browser to drive one, so that leg is documented-pending, same posture as other phases' hardware/device legs (see `docs/v2/phases/phase-v2-12-frontend-modernization/gate-result.md`). |
| 2026-07-16 | Profiles / delete (**FIXED**) | **Deleting any profile returned `500 internal server error`** — including a brand-new test plan nothing had ever referenced. Reported by the owner the day the delete action shipped. Diagnosed from the pilot's API log, which named it outright: `profiles: check profile use failed error="ERROR: column \"profile_id\" does not exist (SQLSTATE 42703)"`. | `profileInUse` (the guard that refuses to delete a plan something references) queried **`vouchers.profile_id`**. That column does not exist: a voucher inherits its plan from its **batch** (`voucher_batches.profile_id`, migration 0202) and carries only `batch_id`. Postgres raised 42703 on **every** call, so the guard never returned an answer — it returned an error, which the handler correctly turned into a 500. The query also omitted `card_payments.profile_id` (a `NO ACTION` FK), which would have surfaced as a raw FK violation → another unexplained 500. **Why nothing caught it:** `internal/profiles` had **zero test files**, and the guard hand-writes SQL against four tables owned by *other* phases (0100/0202/0302/0304). Nothing ever executed the statement, so a wrong column name and a right one were indistinguishable until an operator hit it. A guard that cannot execute is worse than no guard: it fails closed on the deletes it should allow, and never once evaluates the reference it exists to find. | Query fixed (`voucher_batches`, plus the missing `card_payments` check). New DB-backed suite `internal/profiles/store_db_test.go`: `TestProfileInUse_QueryExecutes` is the cheap regression test for the whole class — it asserts only that the statement *runs* — and `TestProfileInUse_FindsEachReference` plants one real reference at a time (on an otherwise-unreferenced plan, so a misspelled table cannot pass on someone else's reference). Verified by reverting the fix: the suite reproduces the exact production 42703. |
| 2026-07-16 | Panel / NAS (**FIXED**) | The NAS screen's per-service **live user count was always 0**, on every NAS, since v2 phase 1 shipped. Reported by the owner. | `radius.SetServiceLiveCounts` (then `SetServiceLiveCounter`) was **defined in v2 phase 1 and never called by anything**. Its three siblings (`SetLiveCounter`, `SetNASLiveCounter`, `SetPoolUsageCounter`) are wired in `live/module.go`; this one was missed. The seam is silent by construction — an unset counter deliberately degrades to `0` so the panel renders before the live module boots — so **"never wired" and "nobody is online" are indistinguishable at runtime**, and nothing failed, logged or tested. | Wired in `live.Register`, implemented as `live.ServiceCounts()` (one live-state read for the whole page, bucketed by `nas_service_id`, rather than a per-instance Redis set: a stale set would show phantom sessions on an empty zone, which is worse than a scan on an operator page). Guarded by `TestRegisterWiresEveryRadiusSeam` + `radius.ServiceLiveCountsWired()`, which exists precisely because a dead seam is otherwise invisible. |
| 2026-07-16 | Ops / process (**MY FAULT**) | A session named **`probe-acct`** with IP `10.9.9.9` and ↓12.1 KB/↑53 KB appeared in the pilot's live sessions and history. | Not a bug — **a diagnostic Accounting-Request I injected into the production database** while proving the accounting pipeline worked (`radclient` → `127.0.0.1:1813` via the `localhost` client). It did exactly what a real packet does: created a session row, live entry and usage point. The pipeline was innocent; the test data was mine. | Removed (session + usage_points + acct_dedup + live entry). `pipeline_counters` deliberately left at `received=2`: it counts packets the pipeline really did receive and persist, and editing it to hide my probe would corrupt the one number whose entire value is that nobody edits it (M2 evidence). **Rule going forward: never generate synthetic traffic against a customer's live stack** — prove the pipeline on a scratch DB, or read the existing counters. |
| 2026-07-16 | Tests (accounting) | The `internal/accounting` chaos suite passes on a **fresh** database and fails on a **second run against the same one**, with `counter invariant broken after flood` — a confusing symptom that looks like a lossless-pipeline (M2) regression and is not one. **Pre-existing** (verified: identical first-pass/second-fail at `eebd149`, before v2 phase 1). | FR-40's invariant `received − duplicates − in_queue = persisted` is checked against the **durable** `pipeline_counters` mirror, which is cumulative across the whole database's life. The suite's per-test cleanup does `rdb.Del(streamKey)`, discarding any queued-but-undrained entries that were already counted as `received` — so the DB's cumulative counters stay permanently unbalanced for every later run. Production never deletes its stream, so the invariant is sound where it matters. | Open, test-only. **Run the accounting suite against a fresh DB** (CI does: it creates new service containers per run, so CI is unaffected). If it fails, drop and recreate the test database before debugging anything. Proper fix = scope the invariant assertion to a per-test counter delta rather than the cumulative mirror; not attempted in this phase, which never touched the counters. |
| 2026-07-16 | NAS auto-setup (multi-hotspot) (**PARTIALLY FIXED — v2 phase 3, 2026-07-17**) | On a NAS running **several hotspot zones with their own profiles**, FR-56 auto-setup refuses with a `/ip/hotspot/profile` conflict ("auto-setup only targets the default hotspot profile") instead of configuring them. The FR-14 copy-paste snippet handles the same router fully — it addresses each zone by its own server name. | `planHotspotProfile` only ever matches the stock profile named `default` (a v1 safety decision: refuse rather than guess which profile a zone uses). v2 phase 1 made the *snippet* multi-service (C8) and made auto-setup plan each service kind additively, but did not teach the hotspot planner to read the router's real profile layout. | Split by how the router got that way, per v2 phase 3 (FR-65-67, `docs/v2/phases/phase-v2-3-autosetup-config-manager/00-phase.md`): **(a) a router still on the stock `default` profile** — FR-66's modify-or-create resolution can now `update` a resolvable conflict in place (still abort-by-default; the hotspot-profile conflict itself stays `Resolvable: false` because there is still no *single* target when a custom profile exists — this case is unchanged). **(b) a router with a custom-named zone profile, or any NEW zone HikRAD provisions** — FR-67's server management gives every zone its own dedicated profile from creation (never "default"), so the ambiguity this bug exploited cannot arise for anything created or adopted through HikRAD going forward; **adopt** lets the operator pick the exact existing zone and edit its real profile directly, which is the actual fix for a *pre-existing* custom-profile router (there was never a safe way to auto-detect which zone "belongs" to HikRAD, so this is deliberately operator-driven, not automatic). The v1 all-default-profiles case (this row's original bug) is unaffected either way and still fails safe. Locked by `TestPlanAutoSetup_MultiHotspot_ProfileLimitation` (original limitation) + `TestPlanService_CreateHotspot_IncludesFR627Guard`, `TestPlanService_Edit_ExistingMatch_ProducesSetNotAdd` (v2 phase 3 fix paths). |
| 2026-07-16 | NAS discovery | MNDP broadcast discovery cannot work under Docker Desktop (dev machines) and requires the HikRAD host to share the routers' L2 segment even in production. | MNDP is a LAN broadcast to UDP 5678; Docker Desktop's VM never receives host-LAN broadcasts, and routed/VLAN-separated routers never reach the server's segment at all. | By design. Production: UDP 5678 is now published (see fix below) and same-L2 works. Otherwise use the panel's IP-range scan, which works everywhere. |
| 2026-07-16 | Portal | A subscriber with `has_password=false` (passwordless hotspot login, item 13) cannot log into the portal with a password. | Deliberate: letting "" match "" would open every passwordless account to anyone knowing a username. | By design; portal login for such accounts needs a future credential (e.g. OTP via WhatsApp — candidate v2+ item). |
| 2026-07-16 | Update | Panel "update" is guided-only: it shows the version and the exact host command, but cannot run the update itself. | The panel container cannot restart the stack containing itself; a privileged host daemon is required. | Kicked off 2026-07-18 as v2 phase 7, FR-86–88 (docs/v2/07-one-click-updater.md; label corrected from an earlier "v2 phase 5" reference predating Decision 35's renumbering — v2 phase 5 is closed-source distribution, a different phase). |
| 2026-07-16 | Repo history | The git history contains a ~75 MB `go1.24.0.linux-amd64.tar.gz` and a compiled `harness.exe` from an accidental commit (removed from the tree 2026-07-16). | Committed by a bulk `git add` during Phase-5 gate work on Windows/WSL2. | Tree is clean; history rewrite deliberately NOT done (would break every clone). Revisit only if repo size becomes a real problem — coordinate with all clones. |
| 2026-07-16 | Restore/update | `hikrad restore` full round-trip and `hikrad update` rollback have never been exercised on live hardware (documented-pending from the Phase-5 gate). | Gate ran out of pilot-hardware time; scripted equivalents pass. | Open — exercise during the pilot (docs/phases/phase-5-v1-reports-install-license/gate-result.md item 3). |
| 2026-07-16 | RADIUS / pools (Hotspot) | A Hotspot login for an `allow_hotspot` subscriber fails on the router with **"no more free addresses in the pool"** (client gets no usable address). Reported from the pilot. | v1's authorize reply **unconditionally** returns the subscriber profile's pool as `Framed-Pool` for Hotspot sessions too (`replyIntents`, `internal/radius/policy.go`). A MikroTik Hotspot client already has a DHCP address before RADIUS runs; returning `Framed-Pool=<profile-pool-name>` makes the hotspot try to allocate from an `/ip pool` **of that exact name on the router**, which either doesn't exist or is empty/exhausted → login fails. The profile's pool is a PPPoE pool; v1 has no per-service pool and never omits `Framed-Pool`. (Not a NAS-binding issue — v1 doesn't scope subscribers to a NAS; the coupling is the reply pool.) | **Code fix shipped in v2 phase 1 FR-64.3** — service-aware pool precedence: Hotspot omits `Framed-Pool` unless a hotspot-service pool is explicitly set, so the router uses its local pool (`docs/v2/phases/phase-v2-1-hotspot-management/00-phase.md` C6, gate item 6). **Still open at the pilot** — see the row below: the reported recurrence is a different failure that shares the message. |
| 2026-07-16 | RADIUS / hotspot (**FIXED on the pilot**) | **"no address from ip pool"** / captive portal "cannot assign ip address - no more free addresses from pool" — persisting **after** the FR-64.3 fix was deployed (pilot VM confirmed at migration 504). Every RADIUS hotspot login on the router failed; local (non-RADIUS) hotspot users on the same box worked fine. | **The router's `default` hotspot USER profile had `address-pool=*1D` — an internal id left behind by a deleted pool.** A RADIUS-authenticated hotspot user lands on the `default` user profile (HikRAD sends no `Mikrotik-Group`), and **the user profile's `address-pool` overrides the hotspot server's own**. So at login the router tried to allocate from a pool that no longer existed and refused. Every signal an operator would naturally check pointed the wrong way: HikRAD's own decision stream said `accept` with **zero reply attributes** (no `Framed-Pool` — HikRAD was blameless), the hotspot server's `address-pool` was healthy, `/ip pool print` showed 1002 of 1013 free, and the client already held a DHCP address (`16.144.96.253`) from that very pool. The working users were all `radius=false` on named profiles (`IT`, `CG-Law`, …) that set no pool — which is why the box looked fine. Two earlier diagnoses in this log (v1's unconditional `Framed-Pool`, then pool-name mismatch) were **wrong for this instance**; both are real bugs, neither was this one. **Worse, the GUI actively lies here:** Winbox resolves a pool id to a name, so a *dangling* id renders as an empty "none" — the owner checked it, was told it was fine, and only setting it to a real pool and back to `none` (forcing a real write) fixed it. | **Router fix (one line, and it must be a real SET — looking at it in Winbox tells you nothing):** `/ip hotspot user profile set [find default=yes] address-pool=none`. **HikRAD hardening shipped:** (1) the FR-14 hotspot snippet now emits that exact line, so a router set up through HikRAD cannot land in this state; (2) **FR-62.7 health check** (`vendor.CheckHealth`, surfaced by `POST /nas/{id}/discover-services`, rendered as a sticky warning in the NAS services editor) detects a `default` hotspot user profile whose pool is missing **or** merely set (it silently overrides HikRAD either way); (3) **`POST /nas/{id}/health/fix`** applies the fix from the panel — the finding cannot be fixed from Winbox, so HikRAD does the write itself after re-verifying the router still reports it. Verified against the live pilot router; confirmed fixed by the owner 2026-07-16. |
| 2026-07-16 | Live sessions / accounting (**FIXED**) | The live-sessions screen showed **"PPPoE" next to a hotspot user's name**, and never showed which hotspot server the session was on. Reported by the owner; confirmed in real data (`{"username":"hikari",…,"service":"pppoe"}` for a session on the `Students-Wifi` hotspot). | A MikroTik sends `Service-Type=Login-User` on a hotspot **Access-Request** but **omits it from Accounting-Requests**. `accounting.pl` defaulted a missing `Service-Type` to `"pppoe"`, and `ResolveService` **filters candidates by that coarse kind before** looking at `Called-Station-Id` — so on this hotspot-only NAS the pppoe filter matched zero candidates and resolution bailed before ever seeing the attribute that identifies the instance. It then fell through to the same wrong hint. The authorize path was unaffected (its `Service-Type` is present), which is exactly why the panel showed a correct hotspot *auth* and a wrong hotspot *session*. | An unknown kind is now **`""`, not `"pppoe"`** (`service_hint()` in `accounting.pl`, `Record.coarseService`), and `ServiceQuery.Service == ""` tells the adapter to identify by attributes alone — the resolved instance's own service is the answer. `resolveService` gained a fallback rung: a NAS whose enabled instances are **all the same kind** records that kind even when the zone is ambiguous. Sessions and live state now also carry `nas_service_id` + the instance's display name, and the panel shows the server under the service badge. Authorize still normalizes an empty kind to `pppoe` so C6 step 2's `nas_not_allowed` is unchanged. |

## Fixed — v2 phase 1 (hotspot management, 2026-07-16)

| Area | Symptom | Root cause | Fix |
|---|---|---|---|
| CoA / ROS quirk matrix | A CoA (rate change, pool move) targeting a **hotspot** session on a NAS that also runs PPPoE would consult the quirk matrix as though the session were PPPoE — silently skipping a supported in-place change, or attempting one known to NAK/hang, depending on the ROS version. Latent in v1 (no NAS could run both services); a live bug the moment FR-62 multi-service NAS lands. | `coaService.send` keyed `adapter.SupportsInPlace(ros, nasType, intent)` off `nas.type` — the NAS's single service. That was a sound proxy for "what kind of session is this?" only while a NAS ran exactly one service. Dropping `nas.type` (migration 0501) forced the question and exposed it. | `SessionRef` gained a `Service` field, filled from the live session's own `livestate.State.Service` at every call site (enforce/renew/refund/tod/live-disconnect); the quirk lookup now keys on the session's actual service. Empty falls back to `"pppoe"` (v1's default), so nothing regresses. Commit: v2 phase 1 chunk 1. |
| Accounting pipeline | **Every session would have been recorded as `service='pppoe'`** after the 0501 migration, silently: FR-58.2's per-service session counting, the live hotspot/pppoe split, and every service-filtered report would all have been wrong, with no error anywhere. Caught only by running the DB-backed suite — the unit suite was green, because the query is raw SQL the compiler cannot check. | `internal/accounting/resolve.go` derived a session's service from `SELECT id, type FROM nas` — i.e. `nas.type`, which migration 0501 dropped (the phase doc's contracts never mentioned this consumer). Worse, the resolver's error branch returns a `{zeroUUID, "pppoe"}` sentinel by design (orphan tolerance), so the missing-column error degraded into a *plausible-looking wrong answer* rather than a crash. | The pipeline now resolves the session's service instance per record through the same `vendor.ResolveService` seam the authorize path uses (C7): `accounting.pl` forwards `Called-Station-Id`/`NAS-Port-Type`/`NAS-Port-Id`/`service_type` raw, `Record` carries them, and `sessions.nas_service_id` (migration **0503**, the range the phase doc reserved for build-time discoveries) records which instance a session ran on — which is also what gate item 4's "live sessions carry the service instance" needs. Fallbacks never drop a record (M2): adapter answer → the sole enabled instance if unambiguous → coarse hint, unattributed. Commit: v2 phase 1 chunk 2. |
| Tests / API integration | `TestIntegration/seeded_subscriber_listed_with_envelope_and_pagination` fails on **every** real-database run, asserting "password material leaked in list response". **Pre-existing since v1.1** (verified failing at `eebd149`, before this phase) — it only ever passes when the DB-gated suite self-skips, which is why it went unnoticed. | The assertion did `bytes.Contains(body, "password")`. v1.1 item 13 added the `has_password` **boolean** to the subscriber read shape (migration 0412); the substring matches that field name. The response never actually contained a credential — the test decodes `password_enc` separately and that check was always passing. | Assertion narrowed to the things that would be real leaks: the sealed column name `password_enc` and the seeded cleartext `testpass`. Commit: v2 phase 1 chunk 2. **Worth noting:** CI's backend job runs against real service containers, so this should have been red — the CI signal for that job deserves a look. |

## Fixed — v1.1 maintenance (2026-07-16, owner's 26-item review)

| Area | Symptom | Root cause | Fix |
|---|---|---|---|
| Vouchers/portal | Every scratch/voucher code entered in the portal said "That code doesn't look right." (item 21) | Printed codes are typed with grouping separators ("ABCD-1234", spaces) — the portal placeholder even suggests a dash — but `hashCode` only upper-cased + trimmed outer whitespace, so any separator produced a different sha256 than the stored one. Compounded by the frontend mapping **any** 422 (including `no_profile` and cooldown edge cases) to the same "invalid code" message, masking the real error. | `normalizeCode` strips every non-[A-Z0-9] char before hashing (generation + redemption); redemption also probes the legacy hash so already-printed stock stays valid (`backend/internal/billing/voucher.go`). Frontend now maps error codes distinctly (`no_profile`, `voucher_void`, generic server error vs network) in both portal panels. |
| Panel | Managers saw "Something went wrong" on pages their role can't access (item 14) | Routes were unguarded — the page mounted, every fetch 403'd, and the generic ErrorState rendered. The Live Sessions nav item wasn't permission-gated at all. | Route-level `RequirePerm` guard rendering a localized no-access page (`frontend/panel/src/auth/RequirePerm.tsx`); every route wired to the same permission the sidebar uses; sessions nav gated on `live.view`. |
| Panel | Header balance only updated on full page reload (item 7) | `BalanceWidget` fetched once per manager id; nothing invalidated it. | `BALANCE_CHANGED_EVENT` fired by every balance-touching API call (renew/refund/topup/voucher create/void/redeem) + refetch on window focus/visibility + 60 s backstop poll. |
| NAS discovery | Auto-discovery without a CIDR never found anything (item 9) | MNDP announcements are LAN broadcasts to UDP 5678; `hikrad-api` sits on the compose bridge network with no published UDP port, so broadcasts never reached the in-app listener. | `deploy/compose.yml` publishes `5678:5678/udp` on hikrad-api (host netfilter DNATs the datagrams through); discovery modal explains the same-L2 requirement when zero results (see open-limitations row for Docker Desktop). |
| NAS | `ros_version` was manual and usually stale/empty (item 8) | No probe existed; version was a free-text field. | `POST /api/v1/nas/{id}/probe` reads version/board/identity over the RouterOS API (read-only, saved credentials) and stores `ros_version`; "Detect" button on the NAS card. |
| Settings | Panel had no version/update visibility; `/system/version` always reported "dev" (item 1) | `appVersion` ldflag was never passed by the Docker build. | `HIKRAD_VERSION` build-arg wired through api.Dockerfile + compose + `scripts/hikrad` (reads repo-root `VERSION` file); new Settings > System screen with guided `hikrad update` walkthrough. |
| Subscribers | No way to create passwordless hotspot users (item 13) | Password was unconditionally required at create; empty credential had no representation. | `no_password` flag on create/update seals an empty credential + `has_password` column (migration 0412); RADIUS auth already compares empty==empty; portal password login explicitly refuses passwordless accounts. |
| Ops | No uninstall path existed (item 12) | Never built. | `hikrad uninstall [--yes|--keep-data|--purge]` (final backup, stack+images removal, cron + CLI cleanup, keeps backups+.env unless --purge) + `scripts/uninstall.sh` wrapper. |

## Fixed — during v1 phases (details in the linked gate results)

| Found | Area | Issue → fix | Reference |
|---|---|---|---|
| 2026-07-16 (P5 gate) | Install | Repo-wide missing executable bits: every shebang'd script stored non-executable in git, breaking FreeRADIUS scripts + install.sh on a fresh clone → bits restored, CI check added | docs/phases/phase-5-v1-reports-install-license/gate-result.md |
| 2026-07-16 (P5 gate) | hikrad-api | Crashed outright on a slow first-boot DB/Redis connection instead of retrying → boot-time retry loop | same gate result |
| 2026-07-15 (P5) | hikrad-acct | Crash-loop: `deploy/data/acct-spill` bind-mount auto-created root-owned → install.sh chowns pre-`compose up` | scripts/install.sh |
| 2026-07-14 (P5) | hikrad-acct | 5 pipeline bugs: PEL stranding, consumer identity, counter durability, etc. → fixed; Redis `appendfsync=always` decision | docs/evidence/redis-durability-decision.md, memory: phase5-agent2-status |
| 2026-07-12 (P4) | FreeRADIUS | Panel-added NAS timed out forever: FreeRADIUS loads clients only at startup; reload can't add them → hikrad-run.sh supervisor restarts on clients-file change | deploy/freeradius/hikrad-run.sh, docs comments in compose.yml |
| 2026-07-11 (P3 gate) | Billing/tests | Test-isolation race + enforcement audit dropped under CoA timeout → both fixed at root | docs/phases/phase-3-billing-security-monitoring/gate-result.md |
| 2026-07-11 (P3) | Platform | Deadlock class: reading `platform.Settings` (pool query) inside an open pgx transaction exhausts the pool under concurrency → rule: resolve settings BEFORE `db.Begin` (see `renewParams.fillSettings`) | code comments in internal/billing/renew.go |
