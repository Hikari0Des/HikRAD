# HikRAD — Product Requirements Document

> Version 1.10 — 2026-07-08 (amended 2026-07-09: FR-55–58 added, Decisions 16–20; amended 2026-07-10: FR-41/FR-44 portal data-display rules, Decision 21; amended 2026-07-11: FR-59 scratch-card payments + FR-60 device monitoring added, v2 backlog established — Decisions 22–24; amended 2026-07-16: v2 phase 1 committed — FR-61–64 hotspot management + NAS scoping, Decision 28; amended 2026-07-17: v2 phase 3 committed — FR-65–67 NAS auto-setup config manager + Hotspot/PPPoE server management, Decision 33; amended 2026-07-17: v2 phase 4 committed — FR-68–70 full multi-currency billing, Decision 34, superseding the implicit-IQD assumption; amended 2026-07-17: v2 phase 4 shipped (gate GREEN 10/10) and v2-9 kickoff blockers resolved — FR-71–76 cost/margin/reseller pricing, Decision 36; amended 2026-07-17: v2 phase 9 shipped (gate GREEN 11/11) and v2-2 kickoff blockers resolved — FR-77–80 manual payment providers, FR-23 e-wallet gateways retired, Decision 37; amended 2026-07-18: v2 phase 5 committed and shipped (gate GREEN 7/7 scripted legs) — FR-81–83 closed-source distribution & licensing hardening, Decision 38; amended 2026-07-18: v2 phase 6 kicked off — FR-84–85 per-manager preferences + subscriber email, Decision 39) — Status: Draft (all decisions user-confirmed)
> Decomposed into domain sub-PRDs: see [docs/prd/00-index.md](prd/00-index.md)

## 1. Overview

HikRAD is a commercial RADIUS AAA + billing platform for ISPs, built first for the Iraqi market. It delivers the feature depth of Snono SAS4 Radius (users, profiles, NAS management, billing, reports) with a dramatically simpler, modern UI and easier UX, and goes beyond SAS4 in one deliberate specialty: **monitoring and live data** — real-time session visibility, NAS/system health, and a lossless accounting pipeline that never drops a usage record. It is sold as a one-time license per server and installed on-premise at each ISP via a Docker-based installer.

## 2. Problem Statement

Small and mid-size Iraqi ISPs (WISPs, building networks, regional providers) run their subscriber authentication and billing on SAS4 or ad-hoc MikroTik User Manager setups. The pain:

- **SAS4's UI/UX is dated and dense.** Staff need long onboarding; daily tasks (renew a user, find why someone is offline, credit an agent) take too many clicks across cluttered screens.
- **Monitoring is an afterthought.** Operators lack a trustworthy real-time picture of who is online, what NAS is struggling, and whether accounting data is complete. Missed accounting packets mean disputed bills and lost revenue.
- **Alternatives don't fit the market.** International billing platforms are priced in recurring USD subscriptions, lack Arabic/Kurdish RTL interfaces, ignore Iraqi payment channels (agent cash collection, ZainCash/FastPay/Qi), and assume reliable internet — a dealbreaker when RADIUS must keep working through upstream outages.

Today ISPs tolerate SAS4's UX because nothing else covers the same feature set for this market. HikRAD's opening: match the core feature set, beat it decisively on UX and monitoring, and sell on the licensing model Iraqi ISPs already prefer (one-time purchase).

## 3. Goals & Success Metrics

### Goals
- Ship a v1 an Iraqi ISP can run its entire prepaid subscriber business on: AAA, profiles, billing, vouchers, live monitoring.
- Make every daily operator task achievable in ≤ 3 clicks from the dashboard, with zero training beyond a one-page guide.
- Capture 100% of RADIUS accounting data, durably, even under NAS floods, DB restarts, or upstream outages.
- Be installable by a mid-level network tech on a clean Linux server in under 30 minutes.

### Non-goals (explicitly out of scope for v1)
- Prepaid card system with drag-and-drop card designer (Phase 2 — one-time *vouchers* are in v1; printable card batches/designer are not).
- Infinite reseller/sub-manager tree with balance transfer down the chain (Phase 2; v1 has flat managers with granular permissions).
- Native mobile apps. The "app" experience ships in v1 as installable PWAs of the portal and panel (FR-54); a native/TWA store app is only built later if Play Store presence proves necessary.
- Publicly documented third-party API (the API exists in v1 for our own frontends; public docs and stability guarantees come later).
- Cloud/SaaS multi-tenant hosting; non-MikroTik NAS certification; postpaid invoicing; FTTH/OLT management; CRM/ticketing.

### Success metrics
- **M1:** First pilot Iraqi ISP in production, running all subscribers on HikRAD for 30 consecutive days.
- **M2:** Zero lost accounting records at the pilot — every Accounting-Request received is durably stored (verified by the pipeline's own audit counters, FR-40).
- **M3:** Pilot operators use the live-monitoring dashboard daily (it is the answer to "why is customer X offline?", not SSH into the MikroTik).
- **M4:** Fresh install to first authenticated PPPoE user in < 30 minutes following the docs.

## 4. Target Users & Personas

| Persona | Role | Technical level | Context & primary need |
|---|---|---|---|
| **Omar — ISP owner/manager** | Buys the license, owns the business | Medium (networking yes, servers somewhat) | Wants total visibility: revenue, online users, NAS health, agent balances — at a glance, in Arabic, on any device. |
| **Sara — front-desk operator** | Renews users, activates vouchers, answers "why am I offline?" calls | Low | Needs to find any user in seconds, see their live session/usage, and renew or fix them in a few clicks. Trained in one hour. |
| **Ali — network engineer** | Installs HikRAD, adds NAS devices, defines profiles | High (MikroTik expert, Linux basics) | Needs the installer, NAS wizard with copy-paste MikroTik config, RADIUS debugging tools, and CoA that actually works. |
| **Hassan — field agent** | Collects cash in a neighborhood, credits subscriber accounts | Low (phone-first) | Needs a limited manager account: search his users, renew from his balance, see his own collection report — installed on his phone as the panel PWA (FR-54). |
| **Noor — subscriber** | End customer of the ISP | Low | Portal (Arabic/Kurdish/English) to check remaining days, consumed data, current speed, usage history, and renew via e-wallet or voucher code. |

## 5. User Stories & Key Flows

### User stories (by feature area)

**Authentication & sessions**
- As Ali, I want to add a MikroTik NAS and get a generated config snippet to paste into RouterOS, so that PPPoE auth works on the first try.
- As Sara, I want to see instantly whether a caller's session is online, its live speed, and last-disconnect reason, so that I can answer support calls without escalating.
- As Omar, I want expired users automatically moved to a "expired" pool/profile with a redirect page instead of hard-cut, so that they renew instead of calling.

**Billing & subscriptions**
- As Sara, I want to renew a user with one click (repeat last profile) or switch profiles, so that the counter line moves fast.
- As Hassan, I want renewals to deduct from my agent balance and appear in my collection report, so that my end-of-week settlement with the ISP is automatic.
- As Noor, I want to redeem a voucher code or pay with ZainCash from the portal, so that I renew at midnight without calling anyone.

**Monitoring & health**
- As Omar, I want a Telegram alert within a minute when a NAS goes unreachable or the RADIUS service degrades, so that I hear it from HikRAD before customers call.
- As Ali, I want per-user and per-NAS traffic history graphs with no gaps, so that usage disputes are settled by data.
- As Omar, I want a daily digest (new users, renewals, revenue, expiring soon) so I can steer the business from my phone.

### Key flow 1 — Subscriber authenticates (PPPoE)
1. Subscriber's router initiates PPPoE to the MikroTik NAS.
2. NAS sends Access-Request to HikRAD's FreeRADIUS.
3. FreeRADIUS queries the HikRAD backend: credentials valid? account active (not expired/disabled/over-quota)? simultaneous-session limit ok? MAC lock ok?
4. Backend returns Access-Accept with profile attributes (Mikrotik-Rate-Limit, Framed-Pool or Framed-IP-Address, session-timeout) — or Accept with the "expired" pool attributes if the account is expired and the ISP enabled the redirect-walled-garden option.
5. Accounting Start arrives → session appears in the Live Sessions table within 2 s; Interim-Updates stream usage; Stop closes the session record.

### Key flow 2 — Front-desk renewal
1. Sara types any fragment (username, name, phone) in the global search bar (visible on every page).
2. User page opens: status banner (online/offline, expiry, remaining quota), live session widget, last payments.
3. She clicks **Renew** → dialog pre-selects the user's current profile and price → confirm.
4. Backend: charges (records payment against Sara's manager account), extends expiry per profile rules, and if the user is currently online in the expired pool, fires CoA so full speed resumes immediately — no manual disconnect.
5. Receipt is printable/sendable; the transaction appears in reports in real time.

### Key flow 3 — NAS goes down
1. Health monitor's ping/SNMP probe misses N consecutive checks on a NAS.
2. Alert fires: in-app banner + Telegram/email per alert-routing settings.
3. Dashboard NAS card turns red with downtime duration; Live Sessions marks that NAS's sessions as stale rather than silently dropping them.
4. When the NAS returns, an all-clear notification fires and an interim-accounting reconciliation pass flags sessions with missing Stops.

## 6. Functional Requirements

Priorities: **M**ust / **S**hould / **C**ould / **W**on't (this release). All Musts constitute v1.

### 6.1 User (subscriber) management
- **FR-1 (M):** CRUD subscribers: username, password, name, phone, address, notes, owner (manager), profile, expiry, status (active/disabled/expired), MAC address, static IP (optional).
- **FR-2 (M):** Global instant search across username/name/phone from every screen.
- **FR-3 (M):** User detail page combines: live session state, usage graphs (daily/monthly), session history, payment history, audit log of changes.
- **FR-4 (M):** Bulk actions on filtered user lists: enable/disable, change profile, extend expiry, move owner, export CSV.
- **FR-5 (M):** Simultaneous-session limit and optional MAC-lock per user (auto-learn first MAC, one-click reset).
- **FR-6 (S):** CSV import wizard for migrating subscriber bases from SAS4/other systems (field mapping + dry-run report).
- **FR-7 (S):** Per-user overrides of profile attributes (custom rate limit, custom price on renewal).
- **FR-58 (S):** Dual-service login: a PPPoE subscriber can optionally also authenticate on Hotspot NASes with the same credentials (per-subscriber "allow Hotspot" toggle). The Hotspot session is allowed **in addition to** the PPPoE simultaneous-session limit (+1, at most one concurrent Hotspot session); Hotspot usage counts against the subscription's validity/expiry but **not** its data quota; Hotspot session speed uses a Hotspot-specific rate limit defined on the profile (falls back to the profile's main rate when unset). *(v2 note: FR-61 generalizes the `allow_hotspot` boolean into a `service_type` enum; the "dual" value preserves these exact semantics.)*
- **FR-61 (S) — v2:** Subscriber service type. Replace the FR-58 `allow_hotspot` boolean with a per-subscriber `service_type ∈ {pppoe, hotspot, dual}` (existing base maps losslessly: `allow_hotspot=false → pppoe`, `true → dual`). A **hotspot-only** subscriber is a full subscriber record (name, phone, profile, expiry, usage history, portal login) that authenticates **only** on Hotspot services — a PPPoE attempt rejects `service_not_allowed` (the exact mirror of the flag-off Hotspot rule); its data **quota does apply** (unlike dual's Hotspot quota-exemption, which exists only to protect the PPPoE quota), and its `session_limit` governs concurrent Hotspot sessions directly. `dual` keeps FR-58 semantics unchanged. Named hotspot-only accounts are distinct from FR-22 anonymous vouchers, which carry no subscriber details by design.
- **FR-63 (S) — v2:** Service-type & multi-service panel/UX. Subscriber form uses a service-type selector (PPPoE / Hotspot / Both) in place of the toggle; the bulk action `set_allow_hotspot` becomes `set_service_type`. The NAS page gains a services sub-list (per-service status/session counts) with wizard steps per service (FR-62). New `service_type` filters on user lists, live sessions, and reports. Subscriber and profile forms gain the FR-64 NAS/service assignment selectors.
- **FR-85 (S) — v2:** Subscriber email. `subscribers.email` — a nullable, format-validated contact field alongside the existing phone/name — threaded end to end: panel form + list column, portal self-edit (FR-44), CSV import mapping (FR-6), audit-logged exactly like phone. Contact data only in this phase: no outbound mail pipeline is built (a future receipt/alert email channel remains a candidate feature, not this one).

### 6.2 Profiles (service plans)
- **FR-8 (M):** Profiles define: price, duration (days), download/up speed (Mikrotik-Rate-Limit), data quota (total, or separate down/up) or unlimited, IP pool, simultaneous-session default.
- **FR-9 (M):** Expiry behavior per profile: hard block, or move to "expired" pool (walled garden/redirect) via RADIUS attributes.
- **FR-10 (M):** Quota-exhausted behavior per profile: block, throttle to a configured speed, or move to expired pool.
- **FR-11 (S):** Burst settings (burst rate/threshold/time) and time-of-day rules (e.g., free night quota, off-peak speed boost).
- **FR-12 (C):** Profile change scheduling (upgrade applies at next renewal vs. immediately with proration).

### 6.3 NAS management (MikroTik PPPoE + Hotspot)
- **FR-13 (M):** CRUD NAS: name, IP, RADIUS secret, type (PPPoE/Hotspot), CoA port, SNMP community (optional), location note.
- **FR-14 (M):** Setup wizard generates a copy-paste RouterOS config (RADIUS client, PPPoE/Hotspot AAA settings, walled-garden basics for Hotspot).
- **FR-15 (M):** CoA/Disconnect-Message support against MikroTik: disconnect session, apply new rate limit without disconnect where supported.
- **FR-16 (M):** IP pool management: define pools, assign to profiles/NAS, view utilization %, warn on exhaustion.
- **FR-17 (M):** Vendor-neutral RADIUS core: MikroTik ships as the certified vendor via its dictionary/templates; architecture must not hard-code MikroTik so other vendor dictionaries can be added later (W for certifying other vendors in v1).
- **FR-18 (S):** Hotspot login page template (ISP logo/colors) served for MikroTik Hotspot with voucher-code login.
- **FR-56 (S):** NAS auto-discovery & API auto-setup: discover MikroTik routers on reachable networks (MikroTik Neighbor Discovery / IP-range scan) to pre-fill the FR-14 wizard; optionally apply the generated config directly over the RouterOS API using admin-supplied router credentials (encrypted at rest). Auto-apply always shows a diff/preview before writing, makes only additive HikRAD-scoped changes — it never overwrites or deletes existing router config, and conflicts abort with a report; the FR-14 copy-paste snippet remains the always-available fallback.
- **FR-62 (S) — v2:** Multi-service NAS. A single router can run **multiple Hotspot server instances and PPPoE server(s) at once**. Replace the one-`type`-per-NAS model with a `nas_services` child table — N rows per NAS of (service `pppoe|hotspot`, zone/label, interface note, IP-pool assignment, RouterOS server-name for matching, enabled). At auth the policy engine resolves the **service instance** from the request's RADIUS attributes (per RouterOS behaviour — the vendor adapter owns the attribute→instance mapping, FR-17) and applies that instance's pool/attributes. The FR-14 wizard generates config covering **all** enabled services in one snippet (multiple `/ip hotspot` + PPPoE AAA); FR-56 auto-setup handles them additively. Live sessions, per-NAS graphs, and reports can group by service instance. Migration: each existing NAS's `type` becomes a single seeded `nas_services` row; the `type` column is retired after backfill.
- **FR-64 (S) — v2:** Subscriber/profile → NAS scoping, enforced at auth. Subscribers and profiles can be **assigned to specific NAS devices and service instances** (nullable = any NAS, the v1 behaviour). Assignment is **enforced at RADIUS auth time**: an assigned subscriber authenticating through a non-assigned NAS/service is rejected with a new reason `nas_not_allowed` (visible in the FR-39 debug tail, localized in the panel). Precedence: subscriber assignment overrides profile assignment; both empty = any NAS. IP pools follow the service instance (FR-62) and are resolved **per service**: PPPoE from the pppoe-service/profile pool, Hotspot from the hotspot-service pool only (the PPPoE profile pool is never applied to Hotspot). **When no HikRAD pool applies the authorize reply omits the `address_pool` intent** so the MikroTik falls back to its own locally-configured pool (this fixes a v1 pilot bug where the PPPoE profile pool leaked onto Hotspot logins and the router reported "no free addresses"). Assignment changes fire `InvalidatePolicy` and are carried in the AuthView policy cache.
- **FR-65 (S) — v2:** NAS config inspection (read-only). `GET /api/v1/nas/{id}/config` connects with the saved RouterOS API credentials (FR-56.2) and returns the router's current RADIUS-relevant state — `/radius` entries, `/radius incoming`, `/ppp aaa`, `/ip hotspot profile` AAA fields, walled-garden entries, plus the FR-13.1/v1.1 version probe — as read-only print sentences, audit-logged. The auto-setup wizard's "Current config" tab renders it as a readable diff-source before the operator commits to any values.
- **FR-66 (S) — v2:** Form-driven auto-setup plan input + modify-or-create per item. The FR-56 auto-setup wizard becomes a two-step flow: a **values form** (RADIUS server IP, CoA port, interim interval, walled-garden list, per-service toggles — pre-filled from settings and, where readable, from FR-65's live config) feeds the FR-14.2/FR-56.2 plan instead of hard constants; the copy-paste snippet endpoint accepts the same overrides so both paths describe one desired state. Each conflicting preview item gains a **resolution choice** — `keep existing` / `update to planned value` / `abort` — instead of only aborting; abort-on-conflict remains the default (this is additive to Decision 17's safety contract, not a replacement of it: nothing is ever written without an explicit per-item opt-in, and the apply hash covers the chosen resolutions so a router-state change between preview and apply still aborts). Deletion is never planned or executed by this or any auto-setup path — router-side removal stays manual, by design.
- **FR-67 (S) — v2:** Hotspot & PPPoE server management (Decision 31). Every `nas_services` instance carries a **management mode** — `router` (pre-existing on the router, discovered/adopted, read-only from HikRAD until explicitly adopted) or `system` (HikRAD created and owns its config). Operators can, from a panel form: **list** each instance's real router-side config read live over the API (reusing FR-65); **create** a new system-managed Hotspot or PPPoE server (interface, pool, profile values, RADIUS wiring, walled-garden) planned/previewed/applied through the same hash-gated FR-66 pipeline, then auto-registered in `nas_services`; **edit** a system-managed instance (re-form, re-preview, re-apply, before/after audit); **adopt** a router-managed instance (HikRAD reads its full current config into a form, the operator confirms, mode flips to `system` — always explicit, never silent). Nothing is ever deleted from HikRAD; disabling an instance only stops HikRAD from serving it (FR-62 semantics unchanged).

### 6.4 Billing, payments & vouchers
- **FR-19 (M):** Prepaid recurring model: renewal charges the profile price, extends expiry by profile duration (from expiry date if still active, from now if already expired — configurable).
- **FR-20 (M):** Manager balances: each manager/agent account holds a balance; renewals they perform deduct from it; admins top up agent balances; every movement is a ledger transaction.
- **FR-21 (M):** Manual payments: record cash payments with receipt number, printable receipt (Arabic/English templates).
- **FR-22 (M):** One-time vouchers: generate batches of codes (profile, count, prefix, expiry-of-code); redeemable by operators, via subscriber portal, or at Hotspot login; single-use; batch list shows used/unused; export CSV.
- **FR-23 (M) — RETIRED (v2, Decision 37):** ~~Iraqi e-wallet payments from the subscriber portal via a pluggable gateway interface (ZainCash, FastPay, Qi).~~ Removed entirely — no merchant account/API docs for any gateway ever existed (Decision 29), and v2's manual payment providers (FR-77–80) replace this surface completely. The `PaymentGateway` interface, mock adapter, gateway config UI, and `payment_intents` machinery are deleted, not quarantined. A real gateway integration, if ever wanted, is a fresh brief built from scratch.
- **FR-24 (M):** Full transaction ledger: immutable, filterable by manager/user/date/type, exportable; discounts and manual adjustments are explicit ledger entries, never edits.
- **FR-25 (S):** Refund/cancel-renewal flow with reason, reversing ledger entries and expiry.
- **FR-59 (S):** Telecom scratch-card payment with **manual verification and a trial window** (no carrier API — fully offline-capable, NFR-7): a subscriber submits a Zain/Asiacell (card type list configurable) airtime-card code from the portal and immediately receives a **1-day provisional renewal** ("test internet"); the payment sits `pending` in an admin verification queue until an admin redeems the physical card value and approves (→ full renewal from the trial's start, standard FR-19 path) or rejects (→ reversing ledger entry, expiry rolled back, subscriber returns to expired/deactivated state per FR-9). The subscriber is notified of each state change (pending/approved/rejected) via portal notifications and the FR-55 channels. Card codes encrypted at rest and visible only to the verifying permission; every step audit-logged. **Amended (v2, Decision 37):** the old "rejection blocks new submissions for a cooldown" rule is **superseded** — a rejected subscriber may resubmit immediately, but a retry earns **no** new trial day (trial eligibility is per payment-cycle, resetting only on approval). Scratch cards become one method tile in FR-78's unified Pay screen, sharing FR-79's ticket/timeline/queue machinery with FR-77's provider transfers rather than a separate flow.
- **FR-77 (M) — v2:** Payment provider catalog + per-manager receiving accounts + per-manager method enablement. The owner maintains a catalog of named providers (AsiaHawala, ZainCash, a bank — **name only, no API integration**, NFR-7 unaffected). Each manager (admin/operator/agent/reseller) who owns subscribers enters their **own** receiving-account details (account number/phone/IBAN/recipient name) per provider they accept, and independently toggles which methods — each provider they have an account for, plus scratch-card (FR-59) and voucher (FR-22) — their subscribers may use. A subscriber sees exactly their **owning manager's** (`subscribers.owner_manager_id`) enabled methods; a method the manager hasn't configured an account for (or hasn't enabled at all) never appears — there is no fallback to a global/admin account (Decision 37).
- **FR-78 (M) — v2:** Unified portal Pay screen + transfer-proof submission with attachments. The portal shows **one** payment surface listing every method the subscriber's owning manager enabled (transfer providers, scratch card, voucher) as tiles in one picker — scratch-card entry is no longer a separate screen. A provider (transfer) submission carries amount, transfer reference/date, an optional note, and **file attachments** (receipt screenshot/PDF, stored on local disk under the data directory, size/type-limited, served back only to authorized reviewers with a forced-download `Content-Disposition`, never executed — NFR-7). The **first** submission in a payment cycle grants an immediate 1-day provisional renewal (reusing FR-59's exact trial mechanism); a request that is rejected may be resubmitted immediately but earns no further trial day until the next cycle's first attempt (i.e., until an approval resets eligibility).
- **FR-79 (M) — v2:** Ticket-style review with hierarchy-wide visibility. Every payment request (provider transfer or scratch card) is a **ticket**: a full timeline of every event (submitted, attachments added, provisional granted, approved/rejected with the reviewer's note, who acted, when). A ticket lands in the owning manager's queue; an admin/global manager's queue shows **every** ticket across every agent (searchable/filterable by agent, provider, state, date), while a scoped agent sees only their own subscribers' tickets (`auth.ScopeFilter`, unchanged semantics). Approve runs the standard FR-19 renewal anchored at the trial's start (as FR-59.2 already does), booking ledger entries per the v2-9 wholesale/retail model (FR-74/76) against the owning agent; reject writes a reversing entry and rolls the expiry back per FR-9, with a required reviewer note.
- **FR-80 (S) — v2:** Notifications on both sides of every payment-ticket state change. The **subscriber** is notified at submission (with provisional-day status), approval (renewed until X), and rejection (with the reviewer's note and the no-free-day-on-retry warning) via portal notifications and the FR-55 channels. The **owning manager** is notified the instant a ticket lands in their queue and whenever another manager acts on it instead of them (in-app + panel push, badge count); a manager above them may opt into all-tickets notifications via their v2-6 preferences. Every notification corresponds to a real timeline event (FR-79) — none is invented separately from it.
- **FR-26 (C):** Promo pricing (temporary profile price override with start/end dates).
- **FR-68 (M) — v2:** Currency model. A `currencies` table (`IQD`/`USD`/`EUR` shipped enabled; extensible, not hardcoded) replaces the implicit-IQD assumption threaded through every `*_iqd` column. Every monetary column becomes an integer **minor-unit** amount plus a `currency` code on the same row (IQD's minor-unit digit count is 0, so its stored values are unchanged by the rename). An admin-maintained `currency_rates` table (from, to, rate, effective date) is the **only** source of exchange rates — **no online rate feed** (NFR-7 holds: multi-currency billing works fully offline). Every rate actually used to move money is stamped onto the ledger row that used it, so historical entries never silently re-value when the rate table changes later.
- **FR-69 (M) — v2:** Money flows in the subscriber's/profile's own currency. Profile price gains a currency; a renewal charges in that currency, end to end (ledger entry, receipt, payment record). Manager/agent balances become **per-currency** (a manager can hold an IQD balance and a USD balance simultaneously; FR-20's derived-from-the-ledger rule applies per currency, never summed across one). There is **no implicit conversion**: moving value between a manager's currencies requires an explicit **exchange** — two linked ledger rows (a debit in the source currency, a credit in the target) stamped with the `currency_rates` row used, so the ledger stays a true multi-currency append-only record rather than gaining a currency-blind mixing point. Vouchers keep FR-22's charge-at-generation model unchanged in mechanism, now carrying the batch profile's currency. Refunds (FR-25) reverse in the original currency at the original entry's own historical values — never today's rate.
- **FR-70 (S) — v2:** Multi-currency display & reports. Settings gains a default display currency; the shared amount formatter generalizes from IQD-only to any enabled currency (existing IQD rendering, including Eastern-Arabic-numeral support, is unchanged in output). Reports and ledger reconciliation ([08](08-reports.md)) run **per currency** — a cross-currency total is never silently summed; an optional "converted to display currency" view is offered but always labeled with the rate and its date, distinct from the authoritative per-currency figures.
- **FR-71 (M) — v2:** Plan cost price. Profiles gain a `cost_price` + currency (what the operator pays upstream for one subscriber-month), nullable — a plan with no recorded cost reports margin as **unknown**, never as zero cost / 100% profit. Cost is **versioned** (`profile_cost_history`, effective-dated), never overwritten in place: a renewal's margin is computed and stamped against the cost in force **on the renewal date**, so re-pricing a plan later never retroactively rewrites a past period's reported profit — the same rule FR-68.3/68.4 already applies to exchange rates.
- **FR-72 (M) — v2:** Margin on the ledger. Every renewal ledger row stamps `cost_at_sale` alongside the amount charged; margin (`amount − cost_at_sale`) is always **derived**, never a separately-editable stored number (the FR-19/FR-24 append-only discipline extended to profit, not just revenue). Reports gain per-plan and per-period revenue/cost/margin, reconciling to the ledger exactly as FR-40 already requires of every other reported figure.
- **FR-73 (M) — v2:** Fixed overheads, global and per-site. An admin-entered `overheads` table (uplink, staff, power, rent — never derived) with an optional NAS/site tag; a global (untagged) overhead and a per-site overhead are two different, never-blended figures — the same "every allocation rule is arguable, so the unallocated number stays the source of truth" reasoning FR-72's per-plan gross margin already applies, extended to the question of *whose* overhead a figure represents. Per-plan gross margin (revenue − plan cost) stays exact and is never adjusted by overheads; net margin (gross − overheads for a period, optionally for a site) is always a separate, clearly-labeled period-level figure.
- **FR-74 (M) — v2:** Reseller (wholesale) pricing, flat 2-level, with optional per-subscriber override. A reseller (any manager reselling to their own subscribers) has a wholesale price per profile (`reseller_prices`, versioned like FR-71's cost), optionally narrowed further to one specific subscriber. No sub-reseller tree — resellers are always direct children of the owner (a deliberate, owner-confirmed scope limit; a reseller cannot themselves have resellers beneath them). At renewal the reseller's balance debits their resolved wholesale price while the subscriber is charged the plan's retail price — two numbers FR-27/FR-19 previously conflated into one — landing on the ledger as an explicit pair so "who paid what to whom" is always answerable without inference, never as one row carrying two meanings.
- **FR-75 (S) — v2:** Reseller-facing margin view. A reseller sees their own margin (retail − their resolved wholesale) and balance; they must never see the owner's plan cost or any other reseller's wholesale price — FR-27.2's manager-scoping principle applied to money, where a leak is a commercial breach, not a UX nit. The owner sees the full chain (cost, every reseller's wholesale, retail).
- **FR-76 (M) — v2:** Reseller pricing resolution algorithm. At renewal, the price charged to the subscriber and the price debited from the reseller's balance are resolved independently, most-specific-first: subscriber `price_override` (FR-7, unaffected by this feature) always wins for the subscriber's own charge; the balance-debit leg tries a per-subscriber wholesale override (FR-74) first, then the reseller's plan-wide wholesale price, then falls back to the plan retail price — identical to v1's existing behavior when no wholesale row exists, so every pre-v2-9 agent is unaffected on day one.

### 6.5 Managers, roles & security
- **FR-27 (M):** Manager accounts with granular permission sets (per module: view/create/edit/delete; per action: renew, disconnect, top-up, export) and per-manager user-ownership scoping (a manager sees only their own users) — flat structure in v1; tree in Phase 2.
- **FR-28 (M):** TOTP two-factor authentication (optional per account, enforceable by admin); login rate-limiting and lockout; full audit log of every manager action (who/what/when/before-after).
- **FR-29 (M):** Session management for panel logins (active sessions list, revoke).
- **FR-30 (S):** IP allowlist per manager account.

### 6.6 Monitoring, live data & lossless accounting
- **FR-31 (M):** Live Sessions table: every online session with username, NAS, IP, MAC, uptime, live down/up rate (computed from interim deltas), total session usage; auto-refreshing (≤ 2 s latency from packet to screen); filter by NAS/profile/manager; actions: disconnect (CoA), open user.
- **FR-32 (M):** Dashboard: online-users count (now + 24 h sparkline), total active/expired/expiring-soon subscribers, today's revenue, per-NAS health cards, RADIUS requests/sec, accounting-pipeline status.
- **FR-33 (M):** Per-user usage graphs: daily and monthly download/upload, session timeline; per-NAS and whole-network aggregate traffic graphs. Retention: raw sessions ≥ 12 months, aggregated daily rollups ≥ 3 years (configurable).
- **FR-34 (M):** NAS health monitoring: ICMP probe (latency/loss) always; SNMP (CPU, memory, uptime, port traffic) when community configured; per-NAS status page with probe history.
- **FR-35 (M):** System self-monitoring: FreeRADIUS up/throughput/auth-reject rate, backend API health, DB health, queue depth of the accounting pipeline, disk space — all on an admin health page.
- **FR-36 (M):** Alerts engine: rules for NAS down/up, RADIUS failure spike, accounting-queue backlog, low disk, user-expiring-in-N-days digest, low agent balance; channels: in-app, Telegram bot, email (SMTP), WhatsApp (Business Cloud API); per-rule routing and quiet hours.
- **FR-37 (M):** **Lossless accounting pipeline:** every Accounting-Request (Start/Interim/Stop) is acknowledged only after being durably enqueued; a consumer writes to the DB; if the DB is down, the queue buffers to disk and drains on recovery; duplicates (NAS retransmits) are idempotently deduplicated by (NAS, Acct-Session-Id, record type, event timestamp).
- **FR-38 (M):** Stale-session reaper: sessions with missed interims are marked stale, then closed with a synthesized Stop after a configurable timeout, flagged as "reaped" (never silently deleted).
- **FR-39 (S):** RADIUS debug tool: live tail of auth attempts for a given username/NAS with human-readable reject reasons (bad password, expired, session limit, unknown NAS...).
- **FR-40 (M):** Pipeline audit counters: received/enqueued/persisted/deduplicated totals exposed on the health page, proving M2 (zero loss) at any moment.
- **FR-60 (S):** Infrastructure device monitoring: CRUD for **monitored devices** that are not NASes (wireless APs, switches, servers: name, IP, type, optional SNMP community, location, notes) probed by the same `hikrad-monitor` engine as FR-34 (ICMP always, SNMP when configured), with `device_down`/`device_up` alert rules (FR-36 channels), probe history per device, and a devices health section beside the NAS cards. Devices have no RADIUS role — they never appear in NAS lists, wizards, or FreeRADIUS client config.
- **FR-55 (S):** Subscriber-facing WhatsApp messaging: expiry reminders (N days before expiry) and payment receipts delivered to the subscriber's WhatsApp number using Meta-approved template messages in the subscriber's language; per-subscriber opt-in (phone consent); reuses the FR-36 delivery infrastructure (routing, quiet hours, retry, delivery isolation); internet-dependent, so it queues/skips gracefully per NFR-7.

### 6.7 Subscriber self-care portal
- **FR-41 (M):** Subscriber login (username/password) to a mobile-responsive portal: status, expiry, **consumed data** (the portal never displays the plan's quota ceiling or remaining-quota balance — Decision 21), current speed, usage graphs, payment history, and the subscriber's own subscription/account details.
- **FR-42 (M):** Portal renewal: redeem voucher code; pay via enabled e-wallet gateways.
- **FR-43 (M):** Portal fully localized (Arabic RTL, Kurdish Sorani RTL, English) with ISP branding (logo, name, colors) set in admin settings.
- **FR-44 (S):** Self-service account maintenance: the subscriber can log in and update their own password and contact details (phone confirmation flow) — promoted from Could per Decision 21.
- **FR-54 (M):** Both the subscriber portal and the admin/manager panel ship as installable **PWAs**: web app manifest (per-ISP icon/name from branding settings), service worker with app-shell caching and an offline "no connection" state, HTTPS-served, "Add to Home Screen" install prompt. Push notifications via Web Push where the platform allows (Android fully; iOS after home-screen install). This replaces native mobile apps; an optional TWA wrapper for Play Store distribution is a post-v1 item.

### 6.8 Reports
- **FR-45 (M):** Financial reports: revenue by day/month, by manager/agent, by profile, by payment method; agent collection/settlement report.
- **FR-46 (M):** Subscriber reports: new/expired/expiring, actives by profile, inactive-N-days; all reports filterable, exportable (CSV; printable view).
- **FR-47 (S):** Usage reports: top consumers, per-NAS totals per period.
- **FR-48 (C):** Scheduled report emails (daily digest to Omar).

### 6.9 Platform, install & licensing
- **FR-49 (M):** Docker Compose–based installer: single script on Ubuntu 22.04/24.04 LTS provisions all services; guided first-run wizard (admin account, ISP branding, first NAS, first profile).
- **FR-50 (M):** One-time license: signed license key bound to a server fingerprint, validated offline (no internet dependency for daily operation); grace behavior and re-issue flow for hardware changes.
- **FR-51 (M):** Backup/restore: scheduled DB + config dumps to local path; one-command restore; update mechanism preserving data (versioned migrations).
- **FR-52 (M):** Internal REST API used by all frontends (panel, portal), versioned from day one (`/api/v1`) so Phase-2 mobile apps and eventual public exposure need no rework.
- **FR-53 (S):** Settings module: timezone (default Asia/Baghdad), currency (IQD default, display formatting), date formats, SMTP, Telegram bot token, WhatsApp Business API credentials, expiry/quota behavior defaults.
- **FR-57 (S):** Optional built-in Cloudflare Zero Trust tunnel for remote panel/portal access: a bundled `cloudflared` container behind a Compose profile, **off by default**, with the tunnel token configured in settings; connection status shown on the health page. Strictly a convenience feature — LAN access and every daily operation keep working with the tunnel disabled or the internet down (NFR-7); only Caddy's web surface is ever tunneled, never RADIUS/CoA.
- **FR-81 (M) — v2:** Binary release pipeline & signed offline bundles. The delivery model changes from **source checkout** (customers receive the whole repo; Compose builds images on-prem) to **compiled, signed artifacts**: a CI release job builds `hikrad-api`/`hikrad-acct`/`hikrad-monitor` plus a prebuilt-frontend Caddy image, tags them `vX.Y.Z`, pushes to a private registry, and produces a signed offline bundle (`hikrad-vX.Y.Z.tar`) containing every image the stack needs (HikRAD's own four, plus pinned third-party images so an install is fully offline-capable per NFR-7), a `compose.yml` with `image:` tags (no `build:`), scripts, migrations, and a checksum manifest with a detached signature. `install.sh`/`hikrad update` verify that signature against a public key embedded in the installer **before** anything in the bundle is used, and refuse a tampered or unsigned bundle with no partial effect. The on-server footprint becomes compose file + scripts + `.env` + `data/` — no source tree, no Go toolchain required. The existing source-build path (`git clone` + `docker compose build`) remains available behind an explicit flag for development only.
- **FR-82 (M) — v2:** Licensing hardening — boot verification in every binary, grace semantics unchanged. `hikrad-acct` and `hikrad-monitor` gain the same offline license load/evaluate/periodic-re-verify behavior `hikrad-api` already runs (FR-50): at boot and on the same re-verify cadence, each binary independently evaluates the installed license against the live machine fingerprint and logs/exposes the result. This is **defense-in-depth visibility, never a new blocking point** — FR-50.3's promise (RADIUS auth and accounting keep running through and beyond grace expiry; only panel *writes* are gated) is unchanged, and no code path in this feature may make either binary refuse to start or stop processing on account of license state. License issuance additionally produces a **scoped, read-only registry-pull credential** bound to the same server fingerprint as the license itself, so the one document a customer receives both activates the product and lets them fetch the images it needs — no separate provisioning step. Explicitly out of scope: DRM, obfuscation, or anti-debug work of any kind — on-prem software can never be tamper-proof, and the enforcement here is contractual/practical (no source, signed updates, support cut-off), not technical.
- **FR-83 (S) — v2:** Repo/business hygiene for closed distribution. The canonical GitHub repository stays private; customers and resellers never receive git access, only the artifacts of FR-81. Anything made public is limited to marketing pages and documentation excerpts, never this repository. `docs/ops/release-checklist.md` gains the signing/tagging/registry-push steps as a mandatory pre-tag gate item.
- **FR-84 (S) — v2:** Per-manager preferences. Each manager gets their **own** presentation-level preferences (language, theme, numerals, landing page, table page size, per-alert-class in-app/push notification routing) stored server-side (`manager_preferences`) and synced across devices on login — distinct from FR-53's **global** settings, which stay the tenant-wide defaults every manager starts from. A manager preference only ever overrides presentation; it can never carry a permission, scoping, or money rule (that boundary is absolute — see the phase brief). `GET/PUT /api/v1/me/preferences`, self-only: a manager can never read or write another manager's preferences (403). localStorage remains the offline/pre-login fallback, exactly as the existing FR-43 portal language toggle already works.

## 7. Non-Functional Requirements

- **NFR-1 Performance:** At 5,000 subscribers / ~2,000 concurrent sessions with 5-minute interims (~7 acct packets/sec sustained, 50/sec burst): auth latency < 100 ms at the backend (p99), accounting ingest keeps queue depth near zero, panel pages load < 1.5 s, live-session updates ≤ 2 s end-to-end.
- **NFR-2 Reliability:** RADIUS auth keeps working if the web panel is down; accounting is never lost across restarts of any single component (queue is disk-backed); target service availability 99.9% on a single server; unclean shutdown must not corrupt data.
- **NFR-3 Hardware footprint:** Runs fully on one modest server: 4 vCPU / 8 GB RAM / 200 GB SSD for the 5k tier.
- **NFR-4 Security:** Passwords hashed (argon2id) — note CHAP/MS-CHAP support for PPPoE requires reversible storage of subscriber RADIUS passwords: store encrypted-at-rest (AES-GCM, key in server config) and document the tradeoff; TLS on all web surfaces (bundled reverse proxy + Let's Encrypt or self-signed); RADIUS secrets encrypted at rest; audit log immutable; OWASP ASVS L2 for the web layer; rate-limited portal login.
- **NFR-5 Usability:** Every daily operator task ≤ 3 clicks from dashboard; keyboard-first global search; a new front-desk operator productive within one hour using a one-page guide; mobile-responsive panel (Hassan's phone).
- **NFR-6 Localization:** All UI strings externalized; Arabic and Kurdish Sorani with true RTL layout (mirrored navigation, charts LTR inside RTL pages); English as development baseline; numerals and currency per locale.
- **NFR-7 Offline resilience:** No feature required for daily operation may depend on internet access (license checks offline per FR-50; e-wallet payments are the only online-dependent feature and fail gracefully).
- **NFR-8 Maintainability:** Solo-dev-friendly: monorepo, one backend service + workers, migrations automated, seeded demo data, CI running unit + integration tests (including a RADIUS packet-level test harness simulating a MikroTik NAS).

## 8. Technical Architecture

**Stack (agent-recommended, user-confirmed):** Go backend · FreeRADIUS 3.2 · PostgreSQL 16 + TimescaleDB · Redis · React 18 + TypeScript · Docker Compose.

Rationale: FreeRADIUS removes all RADIUS protocol risk (same foundation as SAS4); Go gives a single static binary, cheap concurrency for the accounting pipeline and health probes, and low footprint for on-prem installs; TimescaleDB turns usage history into cheap hypertables with automatic rollups (the lossless/graphs requirements are time-series problems); Redis holds live-session state and the fast layer of the accounting queue; React+TS with an RTL-capable component library (e.g. MUI/Ant with RTL, or Tailwind + Radix with logical properties) serves both panel and portal.

```
                     ┌────────────────────────── ISP server (Docker Compose) ─────────────────────────┐
 MikroTik NAS ──1812─► FreeRADIUS 3.2 ──rlm_rest──► hikrad-api (Go)───────► PostgreSQL 16 + Timescale │
 (PPPoE/Hotspot)     │      │ 1813 acct                 ▲    │ CoA (radclient/udp)      ▲             │
                     │      └─► hikrad-acct (Go)        │    ▼                          │             │
                     │           durable queue ─────────┴─► Redis (live sessions,    rollups          │
                     │           (Redis stream +            queue, cache)                             │
                     │            disk spill)                                                         │
                     │  hikrad-monitor (Go): ICMP/SNMP probes, alert engine ──► Telegram/SMTP/WhatsApp│
                     │  Caddy reverse proxy ──► React admin panel · React subscriber portal ──/api/v1─┘
                     └────────────────────────────────────────────────────────────────────────────────┘
```

- **Auth path:** FreeRADIUS → `rlm_rest` → `hikrad-api` policy engine (validity, quota, sessions, MAC) → attributes back. Sub-100 ms budget; policy data cached in Redis with explicit invalidation on renewals/edits.
- **Accounting path (lossless):** FreeRADIUS forwards accounting to `hikrad-acct`, which acks only after appending to a Redis stream backed by disk spill-over; consumers upsert sessions and insert usage points into Timescale hypertables; idempotency key = (nas_id, acct_session_id, record_type, event_time). Audit counters at every stage (FR-40).
- **Live data:** Redis holds current sessions; the panel subscribes over WebSocket/SSE for the ≤ 2 s Live Sessions experience.
- **Data model (core entities):** `subscribers`, `profiles`, `nas`, `ip_pools`, `sessions` (+ `usage_points` hypertable, `usage_daily` rollup), `managers`, `roles/permissions`, `ledger_transactions`, `payments`, `vouchers` (+ batches), `alert_rules`/`alert_events`, `health_probes` (hypertable), `audit_log`, `settings`, `license`.
- **Integrations:** MikroTik RouterOS (RADIUS + CoA :3799, generated config, RouterOS API for FR-56 auto-setup — API client code confined to the vendor adapter per FR-17), Telegram Bot API, SMTP, WhatsApp Business Cloud API (FR-36 channel + FR-55 subscriber messaging), e-wallet gateways behind a `PaymentGateway` interface (create-payment / verify-callback / query-status per adapter), optional Cloudflare Tunnel via bundled `cloudflared` (FR-57).

## 9. Milestones & Phasing

ASAP pacing, sized for a solo developer with AI assistance; sequenced so something real is testable at every step. MVP = end of **P4**; v1 (sellable) = end of **P6**.

| Phase | Contents | Requirements | Rough size |
|---|---|---|---|
| **P1 — Skeleton & auth** | Repo, Docker Compose, DB schema/migrations, FreeRADIUS wired to Go policy API, first PPPoE Accept against real MikroTik | FR-49, FR-1 (partial), FR-8, FR-13–14, FR-17 | 2–3 wks |
| **P2 — Accounting & live data** | Lossless pipeline, sessions, Live Sessions table, usage graphs, CoA disconnect, stale reaper, dual-service auth, NAS discovery | FR-31, FR-33, FR-15, FR-37–38, FR-40, FR-58, FR-56 (discovery) | 2–3 wks |
| **P3 — Users, profiles, billing** | Full user mgmt + search, profile behaviors (expiry/quota), renewals, manager balances/ledger, receipts, vouchers | FR-1–5, FR-9–10, FR-16, FR-19–22, FR-24 | 3–4 wks |
| **P4 — Managers, monitoring, alerts** *(MVP)* | Roles/permissions, 2FA, audit log, dashboard, NAS/system health, infrastructure device monitoring, alert engine | FR-27–29, FR-32, FR-34–36, FR-60 | 2–3 wks |
| **P5 — Portal & payments** | Subscriber portal (3 languages, RTL), voucher redeem, e-wallet gateway interface + first adapter(s), scratch-card payment flow, Hotspot login page, PWA packaging of portal + panel, subscriber WhatsApp messaging, NAS API auto-setup | FR-41–43, FR-54, FR-23, FR-59, FR-18, FR-55, FR-56 (API apply) | 2–3 wks |
| **P6 — Reports, install & license** *(v1)* | Reports, backup/restore & updates, license system, install wizard polish, CSV import, optional Cloudflare tunnel, docs; pilot ISP go-live | FR-45–46, FR-50–53, FR-57, FR-6 | 2–3 wks |
| **P7+ (post-v1 / v2)** | Card system + designer, reseller tree, TWA wrapper for Play Store (if needed), public API docs, more gateways/vendors. **Committed v2 items with prepared briefs in [docs/v2/](../docs/v2/00-v2-index.md):** Hotspot management (hotspot-only subscriber accounts, multi hotspot/PPPoE servers per router) | (v2 backlog) | — |

## 10. Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| E-wallet gateways (ZainCash/FastPay/Qi) require merchant accounts and have weak sandbox/docs | High | Medium | Pluggable gateway interface (FR-23); v1 can ship with any subset; manual+voucher paths make payments never a launch blocker |
| MikroTik CoA/attribute quirks across RouterOS 6/7 versions | Medium | High | Test matrix on ROS 6.49 & 7.x early (P1–P2); packet-level test harness in CI (NFR-8) |
| Solo dev + "ASAP" → scope creep from SAS4 parity pressure | High | High | MoSCoW is contract: Musts only until pilot; parity items live in the P7 backlog, not v1 |
| Lossless pipeline complexity (dedupe, spill, reconcile) | Medium | High | Build in P2 (early, not bolted on); audit counters (FR-40) verify continuously; chaos tests (kill DB mid-flood) |
| License cracking of on-prem one-time licenses | Medium | Medium | Signed keys + fingerprint (FR-50); accept residual risk — support & updates are the real paid value |
| RTL/trilingual UI doubles frontend effort | Medium | Medium | RTL-capable component library from day one; logical CSS properties; ship Arabic+English first, Kurdish strings before v1 |
| Competing head-on with entrenched SAS4 | Medium | High | Wedge = monitoring/lossless data + modern UX + SAS4 CSV import (FR-6) to lower switching cost |

## 11. Decisions Log

| # | Decision | Choice | Source |
|---|---|---|---|
| 1 | Product identity | HikRAD — SAS4 alternative for Iraqi ISPs, modern UI/UX | User |
| 2 | Business model | Commercial, many ISPs; one-time license/server + paid major updates | User |
| 3 | Scale target | Up to ~5k subscribers per deployment | User |
| 4 | v1 scope philosophy | Core AAA + billing + monitoring first; cards/reseller-tree later | User |
| 5 | Surfaces | Admin panel + subscriber portal in v1; the mobile-app experience delivered as installable PWAs of both (FR-54) instead of native apps — simpler and faster to deploy; TWA/store wrapper only if later needed | User (revised 2026-07-08) |
| 6 | NAS support | MikroTik PPPoE + Hotspot on a vendor-neutral core | User |
| 7 | Billing models | Prepaid recurring profiles + one-time vouchers | User |
| 8 | Payment channels | Manual/agent collection + Iraqi e-wallets (ZainCash, FastPay, Qi) | User |
| 9 | Monitoring scope | Live sessions, lossless accounting, NAS/system health, alerts — all v1 | User |
| 10 | RADIUS engine | FreeRADIUS 3.x + custom backend (not custom protocol server) | User |
| 11 | Tech stack | Go · PostgreSQL+TimescaleDB · Redis · React+TS · Docker Compose | Agent-recommended, user-confirmed |
| 12 | Deployment | On-premise per ISP, Docker installer, offline license validation | User |
| 13 | Languages | Arabic (RTL) + English + Kurdish Sorani | User |
| 14 | Team & pacing | Solo developer + AI tooling; ASAP, phase-gated | User |
| 15 | Success metric | Pilot ISP 30 days in production with zero lost accounting records | Agent-proposed, user-confirmed |
| 16 | Notification channels | WhatsApp (Business Cloud API) added as a fourth alert channel (FR-36) **and** for subscriber-facing messages — expiry reminders and payment receipts via pre-approved templates (FR-55) | User (2026-07-09) |
| 17 | NAS onboarding | Auto-discovery + RouterOS-API auto-setup added (FR-56) with a mandatory diff/preview, additive-only changes (never overwrite existing router config), copy-paste snippet always available as fallback | User (2026-07-09) |
| 18 | Remote access | Optional off-by-default Cloudflare Zero Trust tunnel container (FR-57); daily operation never depends on it (NFR-7 holds) | User (2026-07-09) |
| 19 | Dual-service subscribers | A PPPoE account may also log in on Hotspot (FR-58): +1 session beyond the PPPoE limit (max one concurrent Hotspot), counts against expiry but not data quota, Hotspot-specific rate limit on the profile | User (2026-07-09) |
| 20 | Rejected additions | Application-aware QoS and installing HikRAD on MikroTik containers were considered and **skipped** (post-v1 backlog at most; container install ruled out by NFR-3 sizing) | User (2026-07-09) |
| 21 | Portal data display | Portal shows **consumed** data only — never the plan's quota ceiling or remaining balance (business choice: subscribers see usage, not entitlement); subscribers can self-update details/password (FR-44 promoted C→S) | User (2026-07-10) |
| 22 | Scratch-card payments | Telecom scratch cards (Zain/Asiacell airtime) accepted via **manual admin verification with a 1-day trial window** (FR-59) — no carrier API needed, fully offline-capable; v1: backend+portal in P5(plan Phase 4), panel verification queue in P6(plan Phase 5) | User (2026-07-11) |
| 23 | Device monitoring | Infrastructure device monitoring (wireless APs, switches — FR-60) added to v1: same probe engine as FR-34, lands with monitoring in P4 (plan Phase 3) since the probe engine is built then anyway | User (2026-07-11) |
| 24 | v2 backlog committed | Hotspot management (hotspot-only subscriber accounts via `service_type`, multi hotspot/PPPoE servers on one router) and AsiaHawala/Areeba gateway adapters are **v2** — they would rework Phase-2-built code (subscriber model, policy engine, NAS model) or need merchant accounts; briefs + kickoff prompts in `docs/v2/`. Phases 1–2 stay as built | User (2026-07-11) |
| 25 | v2 execution model | **v2+ work is executed by ONE agent, solo and sequentially — no parallel agent teams.** Phase order, standing rules and migration ranges (0500–0589) in `docs/v2/phases/00-v2-execution-plan.md`; v1.x maintenance migrations continue at 0412+ *(amended 2026-07-17: golang-migrate is strictly linear, so "0412+" died once v2-1 applied 0500–0504 — every new migration now takes the next free number above the repo's current max; ranges are budgets only. See known-issues, migrations row.)* | User (2026-07-16), amendment 2026-07-17 |
| 26 | Post-v1 review scope split | Owner's 26-item review split: applied now as v1.1 maintenance (voucher-code normalization fix, permission-page UX, live balance, MNDP port publish, NAS version probe, passwordless hotspot logins, uninstall command, themes, guided update screen, voucher code length, portal name self-edit); deferred to v2 with briefs (multi-NAS scoping **enforced at auth**, auto-setup config manager, **full multi-currency IQD/USD/EUR** — owner explicitly chose full over display-only, per-manager preferences + subscriber email, one-click updater, closed-source distribution) | User (2026-07-16) |
| 27 | Bug documentation | Every bug found — fixed or open — is recorded in `docs/ops/known-issues.md` (root cause + fix), so later sessions never re-diagnose known symptoms. v2 gates include a docs-accuracy check | User (2026-07-16) |
| 28 | v2 phase 1 — hotspot management + NAS scoping | Committed as FR-61–64: subscriber `service_type` enum (pppoe/hotspot/dual) replacing `allow_hotspot` with hotspot-only accounts (quota **applies** to them); multi-service NAS via a `nas_services` child table (`nas.type` retired) with vendor-adapter-owned service-instance resolution (FR-17); subscriber/profile→NAS-and-service scoping **enforced at auth** with a new `nas_not_allowed` reject reason; missing-pool-anywhere omits `address_pool` so the router uses its local pool. Does **not** invalidate v1's PPPoE-first correctness — pppoe/dual auth semantics are unchanged. Owned by sub-PRDs 04 (FR-61/63 data+UI) and 02 (FR-62/64 + auth enforcement); migrations 0500–0519 | User (2026-07-16) |
| 29 | AsiaHawala/Areeba gateways withdrawn | The AsiaHawala (Asiacell) + Areeba gateway-adapter item from Decision 24's v2 backlog is **withdrawn**, not deferred: owner has no API documentation or merchant sandbox credentials for either gateway. Brief file `docs/v2/02-payment-gateways-asiahawala-areeba.md` removed; v2-7 slot dropped from the execution plan. The Phase-4 `PaymentGateway` interface (contract C3) is untouched and still takes a new adapter as a single package with zero core changes, so this can be re-briefed from scratch whenever docs/credentials exist. Decision 24's other item (hotspot management) is unaffected | User (2026-07-17) |
| 30 | Manual payment providers replace gateways | Online payments are replaced by **transfer-proof payments** (v2-2, `docs/v2/02-manual-payment-providers.md`): owner adds payment providers **by name only** (AsiaHawala, ZainCash, banks — never an API); receiving **account details are per-manager** (the subscriber's owning manager/agent/reseller); the subscriber submits a transfer proof **with file attachments** from the portal and immediately gets a **1-day provisional renewal**; the owning manager reviews and approves (full month from the trial's start, FR-19 path) or rejects (rollback) — i.e., FR-59's scratch-card machinery generalized to named providers. After this ships, no HikRAD feature requires internet (strengthens NFR-7). *Clarified same day:* requests are **P2P-style tickets** with a full event timeline; after a rejection the subscriber **may retry immediately but gets no second free day** (trial eligibility resets on approval — supersedes FR-59's cooldown-blocks-submissions rule); **managers above the owning agent see the whole payment log** (admins all tickets, scoped agents their own — ScopeFilter semantics); the portal gets **one combined Pay screen** — providers, scratch cards and vouchers as tiles of a single flow, no separate scratch screen; **each manager enables and specifies the methods** their subscribers can use; **both sides are notified at every state change** — subscriber on pending/approved/rejected (portal + FR-55 channels), owning manager on ticket arrival and on actions taken by others (in-app + panel push, prefs via v2-6) | User (2026-07-17) |
| 31 | Hotspot/PPPoE server management | v2-3's scope grows from *RADIUS config* auto-setup to **server management**: HikRAD can list the router's existing Hotspot and PPPoE servers (**router-managed**, adopted read-only into `nas_services`) and create/edit its own (**system-managed** — provisioned through the RouterOS API and tracked as HikRAD-owned), so an operator can stand up a new hotspot zone or PPPoE service from a panel form. Deletion stays manual on the router, by design | User (2026-07-17) |
| 32 | v3 merged into v2 | The short-lived v3 parking backlog (2026-07-17, same day) is **merged into v2 and removed**: per-manager custom dashboards → v2-10, instance branding (logo + name) → v2-11, and the frontend modernization pass → v2-12 (scope broadened: not just scrollbars — dropdowns/selects, text fields, checkboxes/ticks, radio/toggles and other legacy-looking controls are all restyled into one modern system, plus the responsive/overflow audit; runs LAST so no later phase un-modernizes it). `docs/v3/` deleted; there is one backlog again. *(Labels corrected 2026-07-17 by Decision 35 — originally recorded as v2-8/v2-9/v2-10; unchanged in scope or build order, only the v2-N label.)* | User (2026-07-17) |
| 33 | v2 phase 3 — NAS auto-setup config manager + server management | Committed as FR-65–67 (Decision 31's scope, elaborated at kickoff): read-only config inspection (FR-65) feeds a form-driven auto-setup wizard (FR-66) whose preview items gain a per-conflict resolution choice (`keep` / `update` / `abort`) — this is **additive to Decision 17's abort-on-conflict safety contract, not a replacement**: abort stays the default, an update is always an explicit opt-in, audited before/after, and the apply hash covers the chosen resolutions so a router-state change between preview and apply still aborts. FR-67 adds Hotspot/PPPoE server management on top: every `nas_services` row carries a management mode (`router`-discovered vs. `system`-owned), with create/edit/adopt panel flows through the same hash-gated pipeline. Deletion is never a planned or executed operation — router-side removal stays manual. Owned by sub-PRD 02; migrations 0520–0529 | User (2026-07-17) |
| 34 | v2 phase 4 — full multi-currency billing | Committed as FR-68–70 (item 16, owner's explicit choice of full multi-currency over a display-only dropdown, restated from Decision 26): `IQD`/`USD`/`EUR` (extensible catalog), every `*_iqd` money column becomes an integer minor-unit amount + a `currency` code on the same row, manager/agent balances become per-currency with **no implicit conversion** (an explicit exchange is two linked ledger rows stamped with the admin-maintained rate used), and reports/reconciliation run strictly per currency. **No online rate feed** (NFR-7 unaffected — rates are admin-entered, same posture as FR-56/FR-23's no-internet-required design). This supersedes v1's implicit-IQD assumption everywhere it appears (ledger, balances, profiles, vouchers, receipts, the shared amount formatter) — the reason it is v2 and not a maintenance patch: it reworks the money core Phase 3 froze. Owned by sub-PRD 05; migrations 0530–0539 (v2-9's cost/margin work follows immediately at 0540–0549, same money core, not re-migrated twice) | User (2026-07-17) |
| 35 | v2-N labels corrected to match source file numbers | The `v2-N` phase labels used throughout `docs/v2/phases/00-v2-execution-plan.md` (established 2026-07-16) had drifted from the source brief filenames (`docs/v2/0N-*.md`) — e.g. `v2-2` had come to mean `03-nas-autosetup-config-manager.md` and `v2-7` meant `02-manual-payment-providers.md`, a mismatch the owner caught by simply looking at the file listing. **Every `v2-N` label is renumbered to equal its source file's number** (`docs/v2/0N-*.md` → phase `v2-N`; there is no file `08`, so the label sequence has a gap at `v2-8` rather than renaming around it). This relabels, but does **not** change, the dependency-driven **build order** already under way: hotspot-management (v2-1, done) → NAS auto-setup config manager (now **v2-3**, was mislabeled v2-2 — already built and gated GREEN under the old label; folder/gate script renamed, contents unchanged) → multi-currency (now **v2-4**, was mislabeled v2-3 — docs-only at the time of this decision, no code yet) → cost/margin/reseller pricing (now **v2-9**, was v2-3b, same immediately-after-v2-4 sequencing) → preferences (v2-6, was v2-4) → one-click-updater (v2-7, was v2-5) → closed-source distribution (v2-5, was v2-6) → manual payment providers (now **v2-2**, was v2-7 — this session's original request, not yet started, still genuinely last-but-two in build order because its ledger booking needs v2-4/v2-9's currency + wholesale-pricing work first, per Decision 30/`docs/v2/02-manual-payment-providers.md`) → custom dashboards (v2-10, was v2-8) → instance branding (v2-11, was v2-9) → frontend modernization (v2-12, was v2-10, still last). `docs/v2/phases/00-v2-execution-plan.md`'s table is the authoritative build-order reference; it now states explicitly that its row order, not the `v2-N` numeric order, is the build sequence | User (2026-07-17) |
| 36 | v2 phase 4 complete; v2-9 kickoff blockers resolved | **v2 phase 4 (multi-currency billing, FR-68–70) shipped** — gate GREEN 10/10, `docs/v2/phases/phase-v2-4-multi-currency/gate-result.md`; migrations 0530–0538 (0539 unused/reserved). **v2-9's three kickoff blockers are resolved**, committing FR-71–76 (owned by sub-PRD 05, same as FR-68–70 — no domain split, per the FR-65–67/68–70 precedent): (1) **sub-resellers: no** — flat 2-level only (owner → reseller → subscriber), so reseller pricing is a flat override table, never a recursive tree, and FR-27's existing manager scoping is sufficient with no new hierarchy/ancestry model; (2) **overheads: per-NAS/site as well as global** — `overheads` gains a nullable `nas_id` (null = whole-business), with per-site net margin explicitly never blending in a pro-rata share of global overheads (same "every allocation rule is arguable" reasoning FR-73 already applies to per-plan margin); (3) **reseller wholesale pricing needs per-subscriber overrides** — `reseller_prices` gains a nullable `subscriber_id`, resolved subscriber-specific-then-plan-wide-then-retail (FR-76). `docs/v2/09-cost-margin-and-reseller-pricing.md` updated in place with all three answers and the FR-71–76 renumbering (its original `FR-A..FR-F` / "renumber as FR-6x" note predates v2-4, which has since taken that range) | User (2026-07-17) |
| 37 | v2 phase 9 complete; v2-2 kickoff blockers resolved | **v2 phase 9 (cost/margin/reseller pricing, FR-71–76) shipped** — gate GREEN 11/11, `docs/v2/phases/phase-v2-9-cost-margin-pricing/gate-result.md`; migrations 0540–0543. **v2-2's (manual payment providers) two kickoff blockers are resolved**, committing FR-77–80 (owned by sub-PRD 05; FR-78's portal Pay-screen half cross-referenced from sub-PRD 07, same split as FR-59's existing card-payment UI): (1) **no-account fallback: show nothing** — a subscriber never sees a payment method their owning manager hasn't personally configured an account for; there is no inheritance from a global/admin-level account, closing the "money could land in the wrong person's account" risk the alternative would have opened; (2) **the Phase-4 `PaymentGateway` interface is removed entirely**, not quarantined — `internal/billing`'s gateway adapters, mock adapter, gateway config UI, and `payment_intents` machinery are deleted (FR-23 retired above), since there is no remaining consumer once FR-77–80 ship and a real gateway integration was always going to be a fresh brief (Decision 29). FR-59 (scratch cards) is amended in place: its old rejection-cooldown rule is superseded by FR-78's "retry allowed immediately, no free day until the next cycle's approval" rule, and it becomes one method tile sharing FR-79's ticket/queue machinery rather than its own separate flow | User (2026-07-17) |
| 38 | v2 phase 5 — closed-source distribution & licensing hardening kicked off | Committed as FR-81–83 (item 17, restated from Decision 26): the delivery model changes from source checkout to **signed, compiled image/bundle distribution** — CI builds and signs versioned images + an offline bundle covering every image the stack needs (FR-81); `hikrad-acct`/`hikrad-monitor` gain the same boot + periodic license re-verification `hikrad-api` already runs, **strictly informational** — FR-50.3's grace semantics (RADIUS/accounting never stop; only panel writes are gated) are explicitly unchanged, and license issuance additionally hands out a registry-pull credential bound to the same fingerprint (FR-82); the repo stays private with release-checklist hygiene added (FR-83). **Explicitly out of scope, restated from the source brief:** no DRM, obfuscation, or anti-debug work — accepted residual risk, same posture as Decision-log-owned "License cracking" risk in sub-PRD 01. Owned entirely by sub-PRD 01 (no cross-domain split, same precedent as FR-65–67/68–70); this phase is re-ordered ahead of v2-6/v2-7 in the execution-plan's row order per this session's explicit request — v2-6/v2-7 remain deferred, not cancelled, and this phase's dependency note ("v2-7 before or with v2-5's registry work") is superseded: v2-5 now lands first and v2-7 (one-click updater) will build on its registry/bundle plumbing instead of the other way around | User (2026-07-18) |
| 39 | v2 phase 6 — per-manager preferences + subscriber email kicked off | Committed as FR-84–85 (items 18 and 22-remainder, restated from Decision 26 — name/phone self-edit already shipped in v1.1, email was the remaining piece of item 22): managers gain **their own** server-stored, cross-device presentation preferences — language, theme, numerals, landing page, table page size, per-alert-class notification routing — via a new `manager_preferences` table and `GET/PUT /api/v1/me/preferences`, self-only; strictly presentation-level and **never** a channel for permissions, scoping, or money rules (FR-84). Subscribers gain a nullable, format-validated `email` contact field threaded through the panel form/list, the portal's existing FR-44 self-edit, and the FR-6 CSV import mapping (a SAS4 export column) — contact data only, no outbound mailing pipeline this phase; a future receipt/alert email channel stays a separate candidate feature (FR-85). FR-84 owned by sub-PRD 01 (alongside FR-53's global settings — the same module, a per-manager override layer on top); FR-85 owned by sub-PRD 04 (alongside the existing phone/name contact fields, same elaboration pattern as FR-1.3's phone). Exact JSONB preference schema + versioning, `/me/preferences` request/response shapes, and the email validation rule are frozen in `docs/v2/phases/phase-v2-6-preferences/00-phase.md`, not here — same split as every prior v2 phase's FR vs. `00-phase.md` (FR-81.5 precedent) | User (2026-07-18) |

## 12. Open Questions

1. **Gateway priority:** which of ZainCash / FastPay / Qi gets built first depends on which merchant account HikRAD (or the pilot ISP) can actually obtain — decide when pilot ISP is selected (target: during P4).
2. **Pilot ISP:** which ISP hosts the pilot deployment (drives the ROS version test matrix and Kurdish-language priority).
3. **Price point** of the one-time license — market decision, not needed before P6.
