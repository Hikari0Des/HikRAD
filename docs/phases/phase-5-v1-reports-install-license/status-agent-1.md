# Phase 5 — Agent 1 (Platform & Security) status

**Done.** License (FR-50, C4): Ed25519 verify (`internal/platform/license`, pure/no-DB), fingerprint = hash(machine-id)+hash(primary-MAC) joined, tolerates a single component changing (VM clone/disk migration) before entering grace; 14-day grace state machine; `expired_grace` global middleware (`httpapi.licenseGate`, wired into the frozen chain) blocks mutations but exempts `/internal/*`, `/api/v1/license`, `/api/v1/setup/*`, `/api/v1/auth/*`; grace entry writes an `alert_events` row (monitorsvc's documented rule-less fallback shape, no import needed). First-run wizard (`internal/platform/setupapi`, a sibling of `platform` since `auth`→`platform` already exists and platform can't import auth back) gates on `no manager exists`. Vendor keygen/issuer at `scripts/license-tool/` — separate Go module, outside `backend/`'s Docker build context entirely, not just excluded from the final image stage.
Backup/restore/update (FR-51, C5): `scripts/hikrad` does real `pg_dump`+gpg-AES256 encrypt, retention pruning, TimescaleDB-aware restore (`timescaledb_pre/post_restore`), schema-version forward-compat refusal, and update with pre-backup + image-tag rollback on failed healthcheck. Tunnel (FR-57, C7): `cloudflared` behind compose profile `tunnel`, token decrypted via a new `hikrad-api print-tunnel-token` subcommand (never touches bash), `/etc/machine-id` bind-mounted read-only so container recreation never looks like new hardware. Settings groups completed generically over the existing flat `settings` table (reuses billing's/locale's own keys, no second source of truth); data-retention floors (12mo/3yr) enforced server-side. ASVS L2 pass recorded in `docs/ops/security-checklist.md` with per-row evidence; added HSTS/hardening headers to Caddy.

**Verified for real:** full backend suite green against a real dockerized Postgres+TimescaleDB+Redis (incl. `-race`-clean logic, though `-race` itself won't build here — no gcc). Live-drove the entire license lifecycle end-to-end over HTTP against a running `hikrad-api` (fresh install → wizard → mismatched-fingerprint upload → grace → forced expired_grace → mutation blocked, reads/`/internal` unaffected → re-issue clears) and found+fixed two real bugs this way: (1) re-marshaling the license payload through `any` silently alphabetized JSON keys and broke every signature — fixed by preserving `json.RawMessage` end-to-end; (2) `installLicense` pre-computed the grace transition and saved it directly, skipping `RefreshLicenseCache`'s alert-raising comparison — fixed by always saving neutral state then evaluating. Backup→mutate→restore round-tripped byte-correct against real Postgres via a harness that swaps `compose exec` for direct `docker exec` (no built stack in this sandbox). Also caught gpg's `-o`/`-d` argument-order sensitivity live. `install.sh` idempotency/`--domain` templating tested against scratch copies (never mutated the real host — `/usr/local/bin`+crontab writes are real Linux-target operations, correctly untested on this Windows sandbox by design, not by oversight).

**Gaps, honestly:** full-stack `hikrad update`/`hikrad restore` health-check-gated rollback needs real built images — only the DB/CLI logic was exercised, not against a running compose stack. NAS/profile wizard steps and the actual wizard *UI* are Agent E's; `install-guide.md` includes a raw-API appendix so the M4 rehearsal is runnable either way. `docs/evidence/`, reports, importer are other agents' territory, untouched.

45/45 of my `scripts/gate-phase-5.sh` legs pass (with `HIKRAD_TEST_DB_URL` set — 3 legs correctly FAIL without it, not silently pass-on-skip like a naive `go test` wrapper would).
