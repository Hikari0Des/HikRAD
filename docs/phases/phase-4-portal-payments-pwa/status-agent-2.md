# Phase 4 — Agent 2 (Accounting & Monitoring) status

Done: migration 0330 (push_subscriptions); `internal/push` (VAPID bootstrap/rotation-safe idempotent, RFC 8291 aes128gcm encryption from stdlib+x/crypto/hkdf, subscribe/unsubscribe, 410-Gone pruning, 4 KB keys-only payload cap); `push` wired as monitorsvc's 4th alert channel + per-subscriber expiring digest push; FR-55/FR-59 WhatsApp consumer (`subscriber_events.go`) on `billing.renewed`/`billing.card_payment`, per-subscriber expiry WhatsApp reminder, template catalog in settings; usage API polish in `internal/live/usage.go` (Baghdad-aware month/day bucketing off raw `usage_points`, 800-point response cap, exported `UsageForSubscriber` for D). All proven against real Postgres/Redis + request-capture fakes (no live Meta/browser needed). Gate legs in `scripts/gate-phase-4.sh`, all green.

Bugs fixed along the way: (1) `push.Module.Register` never runs in the separate `hikrad-monitor` process — added `push.Init` called from both `cmd/hikrad-monitor/main.go` and `Module.Register`, or the alert engine's push channel nil-panics in prod. (2) Pre-existing Phase-3 latent bug in `digestSummary`'s `($1 || ' days')::interval` — pgx/PG parameter-type inference makes this silently fail (error was swallowed with `_ =`), so the admin daily digest's "expiring" count has always read 0; fixed both occurrences to `make_interval(days => $1::int)`.

Seams for other agents: **D** — portal push subscribe/unsubscribe route should call `push.Subscribe`/`push.Unsubscribe`/`push.DeliverToSubscriber` directly (no subscriber-auth hook built here, since D's portal token middleware didn't exist yet when this was written); portal usage endpoint should call `live.UsageForSubscriber(ctx, db, subscriberID, monthly, from, to)` instead of re-querying. **F** — push payload i18n keys used: `push.alert.<rule_type>.{title,body}`, `push.expiry_reminder.{title,body}`, `push.payment_receipt.{title,body}`, `push.card_payment.<state>.{title,body}`; confirm/rename as needed.

Not done: browser/Meta-live verification (no such environment here) — proven via request-capture fakes instead, per the task's sanctioned fallback.
