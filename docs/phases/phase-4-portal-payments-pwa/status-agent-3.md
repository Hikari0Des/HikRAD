# Status — Agent 3 (Backend Business), Phase 4

Built: `internal/portalapi` (login/refresh/logout with a separate `portal_access` JWT typ — audience separation is a type-mismatch, not a blocklist — own rate limiter mirroring A's policy, `/me` composition strictly consumed-only per Decision 21, self-update incl. password rotate-all-sessions, usage/payments passthrough, branding). `internal/billing/gateways` (frozen `PaymentGateway` interface; `mock` — HMAC-signed dev simulator wired through the real `processCallback` path; `zaincash` — JWT flow per public docs, unverified against a live account, disabled by default, documented pending FR-23.5). Payment-intent lifecycle (pending→confirmed→renewed/failed/expired, idempotency riding the existing `renewal_idempotency` table keyed by intent id — proven replay-safe with 3x concurrent confirms), reconciliation worker (10 min then hourly, 48h expiry). Scratch-cards (C8): trial/approve(anchored at `trial_started_at`, stored explicitly)/reject(reuses refund's rollback+CoA), one-pending DB constraint, reject cooldown, `billing.renewed`/`billing.card_payment` events. Migrations 0300–0304.

**Real bug found+fixed (pre-existing, not mine originally):** `insertLedger` cast `actor_manager_id` straight to `::uuid` with no `NULLIF`, unlike `subscriber_id`/`reverses_id` on the same line — worked by accident because every prior caller (panel/agent/operator-voucher) always had a manager id. Portal gateway-confirm and card-trial/approve are the first legitimate no-manager callers; fixed by adding the same `NULLIF($3,'')` used elsewhere in that query.

**Seams (narrow additions beyond the frozen C2/C3/C8 routes, since only the listed routes are frozen):** `/portal/refresh`, `/portal/logout`, `/portal/payments/gateways`, `/portal/card-payments/types`, `/portal/card-payments/mine`. F (Agent 4) built against these independently and diffed them against my actual code mid-session — confirmed matching exactly, including nullable `expires_at` and the disabled-subscriber-logs-in-read-only behavior (task brief edge case; F's `disabled` login-outcome branch is intentionally dead code).

Tests: token audience separation, `/me` Decision-21 body-content check, IDOR, callback amount-mismatch + 3x-replay idempotency, reconciliation timing, card trial/approve-anchoring/reject-nets-zero/cooldown — all green on real Postgres+Redis (`go test ./internal/billing/... ./internal/portalapi/...`), plus full `./...` build+vet+test. Gate legs (item 1 API half, 2, 6, 10) appended to `scripts/gate-phase-4.sh`, 39/39 passing standalone.

**Not done:** ZainCash live checklist (no merchant account — explicitly acceptable, FR-23.5). Gateway admin config UI is Phase 5 (E), as scoped.
