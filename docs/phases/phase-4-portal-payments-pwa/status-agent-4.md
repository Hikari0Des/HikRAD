# Status — Agent 4 (Frontend Portal & Localization), Phase 4

Built: real portal auth (own refresh chain, `hikrad.portal.*` storage), home (status/days/consumed-data-only per Decision 21/speed, skeleton+pull-to-refresh), usage+payment history, renew (voucher / e-wallet gateway list+intent poll+return route, deep-link-safe via a persisted pending-intent pointer / scratch-card FR-59 incl. pending+rejected banners), account self-care (FR-44, password warning), branding-aware login+shell. PWA for **both** apps: manifest+SW (precache, network-first API, honest offline banner, no offline mutations), install education (Android prompt + iOS steps), update toast (SW parked in `waiting` until consent), branded-manifest runtime swap. Panel push opt-in + notification click-through wired into the existing bell dropdown. en/ar full translations for every new key; ku drafted (needs native review, same as existing debt). Full suite green: `npm run build/test/lint` (portal+panel) + `i18n:check`; `scripts/gate-phase-4.sh` Agent-4 legs added and passing.

**D/C landed mid-session — verified their real code against my assumed seams (not just re-read, actually diffed):** `/portal/refresh`, `/portal/logout`, `/portal/payments/gateways`, `/portal/card-payments/types`, `/portal/card-payments/mine`, login/me/payments/card response shapes, and optional `profile_id` all match my client exactly (D's own comment in `login.go` cites my client as the spec it built to). Two real fixes made from this check: `expires_at` is nullable server-side (`me.ts`/`HomePage.tsx` now handle it), and `DELETE /push/subscribe` takes a JSON `{endpoint}` body, not a query param (fixed in both `push.ts`s). One confirmed gap, not an assumption: **no HTTP route exists anywhere for the VAPID public key** — `push` only exposes it via a Go call; my panel `pushApi.ts` calls a route (`GET /push/vapid-public-key`) that doesn't exist yet and needs an owner. Also found: disabled subscribers are allowed to log in read-only (never a login-time error) — my `LoginOutcome.disabled` branch is accordingly dead code, left in defensively.

**Panel exception scope:** touched only `public/**` + `src/pwa/**` plus three mechanical one-line wires (`index.html`, `main.tsx`, `notifications.tsx` — one `<PushOptIn/>` mount) per `frontend/panel/src/pwa/README.md`. SW push notification text falls back to English when no tab is open (no build-time locale bundle for a hand-written SW) — flagged there.

**Not done (icons are placeholder SVGs, no real PNG/maskable art; ku needs native review — both pre-existing risks, not new).** Gate items 1/3/4 physical halves (real-phone run, actual device install/offline/update) and live-adapter checklist (item 3) need a human + a running stack with D's backend.
